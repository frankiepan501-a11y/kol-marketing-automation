# Findings

## 2026-08-20 食人花自治补池假成功 P0

- 最新食人花自治任务记录为 `success`，但业务结果是滚动24小时13/120、剩余107、目标可发库存214、库存前后均为0、动作仍为expand。
- 根因不是发送中心限额，而是完成判定只看函数是否抛异常；爬虫任务异步新增KOL后，下一次补池没有用业务结果证明“有候选被刷新/入队”，零进展也会被审计放行。
- 现有小时完成审计只校验 `status=success` 和更新时间，无法识别HTTP成功但补池无产出的业务失败。
- 生产定时任务应继续放在云端n8n；Codex App本地定时会形成第二套调度、依赖本机且难以与唯一发送中心的运行锁统一，因此本次不采用。

## 2026-08-21 三项P0/P1连续实施

- 食人花最新任务的唯一正数是`profile_refresh_writes=30`；当前`_with_business_outcome()`把资料刷新与新增草稿/候选/发现任务同等计为业务进展，因此返回`supply_in_progress`。修复应把资料刷新保留为可观测动作，但不能单独证明供给推进。
- 现有正确在途判定已有测试：`active_pending_before>0`才允许`supply_in_progress`；库存>0仍优先判`ready_inventory_created`。最小修复是在业务进展布尔值中排除`profile_refresh_writes`，同时保留该分项供审计查看。
- 活动参与记录已有`关联邮件草稿 / 关联KOL / 承诺上稿时间 / 实际上稿时间 / 上稿链接`，草稿已有`关联产品 / 是否回复 / 回复意图 / 场景标签 / 回复日期`。无需新建第二套结果表。
- 回复监听已能把`live_link_received`写到草稿场景并回填KOL主表上稿日期；因此活动实际上稿可优先按“参与记录绑定草稿+场景标签+回复日期/主表链接”确定性回填。
- 现有上稿登记卡只绑定KOL主表记录，不携带活动或产品。若一个KOL同时参与多个正式活动，主表上稿日期/链接不能安全归到某一活动；自动回填必须只在唯一活动匹配时写入，多活动歧义则跳过并可见。
- 承诺上稿目前没有结构化事实字段。`感兴趣/要报价/合作状态=洽谈中`不能等同明确承诺日期；需要从明确日期证据或运营确认入口写入，不能默认填活动窗口。
- 首轮线上回放已把假成功正确转成`degraded/supply_blocked`，并暴露真实断点：食人花首批固定拓词已全部使用，动态拓词调用DeepSeek返回`402 Payment Required`，旧代码没有确定性降级词库，因此没有创建任何发现任务。修复不能只停在告警，需在外部模型不可用时仍用受控的Nintendo/Mario收藏、游戏房和硬件评测长尾词创建任务。

### 读取错误

- PowerShell首次解析`+field-list`时误取`data.items`，实际字段数组是`data.fields`；已改按真实返回读取。
- 首次沿用旧KOL主表ID`tblgEeArMSeB7kA2`返回`800030104 not_found`；按当前Base返回改用`tblMMhnj2hEbhF6y`。
- Windows下`rg app/launch_*`把通配符当非法路径；已改为目录搜索配合`-g`过滤，不重复该调用。

## 2026-08-20 食人花候选质量 P1 决策

- P1 执行顺序固定为“先补主表资料维护，再扩候选”，避免把旧的模糊标签直接搬进新审核批次。
- 活动当前 `发送邮件授权=true`；因此 P1 新增参与记录必须强制为 `待审核`，且补池动作和队列/草稿生成动作分离。
- `资料核实时间` 记录本次资料判定时间；当记录仍处于有效的 `人工核实有效` 状态时，不覆盖人工核实结论和人工核实时间。
- 触达路由只表达全局关系状态；同产品重复触达仍由候选预检读取草稿历史做最终裁决，不能只依赖主表单字段。
- 复核发现旧重打标签分支会吞掉飞书写入异常，批量统计仍可能把失败算成功；P1 已统一返回 `write_fail/write_applied`，并把每条记录隔离，单条失败不再中断整批。
- 待审核补池与自动发送彻底分离：只写参与记录，强制 `审核结论=待审核`，创建后回读确认无 `关联邮件草稿`；竞品证据模式为新建/复用时还必须确认排序确实应用证据。
- 生产单条 dry-run 暴露 DeepSeek `402 Payment Required`；不把失败状态写回主表。画像分类增加确定性兜底，按近期标题生成内容垂类/主机生态/IP/活跃度，精确命中官方发行商或媒体时仍按缺资料拦截。

## Feishu Card Callback

- 卡片设计要求：同一张采购卡里每个产品独立输入和提交，提交只影响当前 record。
- 当前代码原来只从 `event.action.form_value` 或 `event.card_form_value` 取表单值；如果飞书真实回调把输入值包在表单名下，或返回 `input_values`/列表结构，旧解析会取不到成本和链接。
- 原卡 GET 读回可以确认展示态和标题，但不一定保留完整表单 JSON，因此不能只靠消息读回来判断按钮/输入框有效。
- 本地 Python 环境存在同名 `app` 包污染；跑仓库单测时必须显式设置 `top_level_dir=os.getcwd()` 或在内联脚本中把仓库根目录放到 `sys.path[0]`。

## KOL Launch P0

- 活动新开发目标不能接纳 `reactivation_same_thread`；旧关系价值应保留在现有流程贡献池或二次发布池，但不能占用新开发人数。
- 人工排除必须驱动状态出池和冷候选补位，不能只写审核备注。
- KOL 主表国家字段可能与近期内容地区冲突；重复出现的非目标地区是冻结信号，单条内容不是。
- 当前 P0 已以 `evidence-v3 / p0-three-pool-v3-20260819a` 回填生产，20 个有效对象均为新开发池；G4 仍关闭。
- 本次审核完成后为 17 条通过、3 条排除；排除尚未驱动自动补位，当前可放行人数只有 17。
- 现有活动配置只关联 60 条 NYXI 帖子；生产库总计 3,423 条，其中既有统计为官方 435、非官方 2,988。所谓“全证据补接”应以 2,988 条非官方帖子为证据池，而不是把官方帖子算作合作证据。
- 60 条样本覆盖 40 位作者；按当前稳定身份键只匹配 24 位 KOL，16 位未匹配。全量接入必须同时报告作者匹配覆盖率，不能只报告帖子数。
- 飞书单个关联单元格无法可靠承载 2,988 条记录；服务端以 `1254001 WrongRequestBody` 拒绝且未产生变更。可靠做法是固定上限分片、连续编号、总数与哈希校验、失败回滚/续跑，并在全量回读后才切换活动排序版本。
- NYXI 全量 2,988 条非官方证据对应 1,457 位去重作者；当前匹配脚本得到 308 位作者/315 条 KOL 主记录，云端运行时因稳定身份实现差异得到 309/316。活动排序的业务口径以帖子数和作者数为硬校验，匹配覆盖率单独披露，不能把“未匹配作者”误当证据丢失。
- 全池预览真实耗时 14分37秒，必须使用后台 job + 状态查询；HTTP 立即受理只代表开始计算，必须回读最终状态、候选数量和证据来源后才可用于补位。
- 参与记录重算时，历史已取消对象仍需进入 `seen` 防止被重新选中，但只有当前 `已入围/锁定准备中` 才能计入本轮流出。混用二者会把历史取消误报为本次缺口。
- 名单开关只允许改变 `名单锁定授权`；不能顺手写 `证据排序版本`。真正锁名单前还要从生产活动重新核对产品、证据版本和全量快照状态。

## 2026-08-23 Dave 可选竞品证据与七层词源灰度

- 最新 Dave 后台任务已越过 NYXI 证据瞬时读取故障，真实新卡点为固定词耗尽：`keyword_source=none`、`shortfall_tasks=4`、可发送库存0。
- 最近12个已完成发现任务记录到13个有效邮箱，但过去24小时活动新开发审核通过0；因此不能把“有效邮箱”当最终供给成功，主指标必须是通过硬筛选后形成的合格/可审核候选。
- 当前代码把主题写成 `dave/piranha`，且只有食人花拥有备用词与动态扩词；这是灰度期技术债，正式模型必须从活动字段读取竞品模式、竞品品牌、产品IP、平台、品类和市场，不能通过产品名隐式绑定 NYXI。
- 通用词池应由七个来源轴交叉生成：可选竞品、IP/主题、适配平台、品类/功能、用户身份/场景、内容形式/达人类型、市场本地化。历史合作/触达关系是优先候选来源和硬过滤，不应降级成普通关键词。
- 新建竞品调查不应阻塞其他六轴：产品和平台词源先运行，竞品证据完成后再追加高优先级候选。
- 第一性原理复核：如果系统只是自动生成词、仍需 Frankie 逐轮批准，就是把人工补词换成 AI 补词的伪自动化；真正完成要靠产出反馈自动保留、扩展、冷却和切换来源。
- YouTube daemon会把`抓取关键词: ...`写入每条新增KOL的`迁移备注`；本轮再把`[词源:competitor/ip_theme/platform/...]`写入爬虫任务名，就能在不新增表/字段的前提下追溯“词源→关键词→新增KOL→有效邮箱”。
- 当前全仓唯一失败`test_run_once_alerts_any_project_service_failed_deployment`在本分支未改动的Zeabur watchdog模块上独立复现；本次不扩范围修复。

## 2026-08-23 剩余P1执行边界

- 飞书KOL结构化业务数据继续以KOL营销管理库为唯一落点；不写Obsidian或本地知识卡。
- KOL Base读写按聪哥分身2号项目路径；写前必须读当前字段结构，不能依赖旧字段名或旧表ID。
- 竞品公开证据只影响优先级，不替代国家、语言、语义、非官方身份、邮箱、双主表重复与目标产品历史硬闸。
- “受控导入”与“活动可发送”必须是两个状态：本P1允许创建主表/参与记录，但不得创建草稿或调用发送器；P1验收后再由既有人工审核/活动规则决定是否放行。
- X公开资料若无法从公开页面稳定取得国家或商务邮箱，必须保留未知并继续拦截，不能从显示名、时区或帖子语言硬猜国家。
- 当前Zeabur生产表配置已回读：Base=`KINabIENjak8fRsB6AHcIDALntc`，KOL主表=`tblMMhnj2hEbhF6y`，媒体人主表=`tblinUWFZHtmXZbC`，活动参与记录=`tblt0zD4hDb7sFqn`，邮件草稿=`tblpWteXNX34vds4`，产品表=`tblate6wgHYWmD6s`。后续只按这些当前值读取字段和回读，不使用旧memory表ID。
- Windows PowerShell下再次验证`rg app/launch_*.py`会把通配符当非法路径；后续只用`rg <pattern> app -g 'launch_*.py'`，不重复旧调用。
- 代码回读确认：统一草稿队列只接收“参与状态=已入围、审核结论=通过、进入方式=新开发、活动分池=新开发池、且未关联草稿”的参与记录；受控导入写成“待审核＋无草稿关联”即可由确定性规则阻断自动发信，写后仍须再次回读。
- X现有官方采集模块已使用`X_BEARER_TOKEN/TWITTER_BEARER_TOKEN`和X API v2，可复用用户资料字段`description/location/url/public_metrics`及帖子`lang`，不需要新增第三方抓取依赖；邮箱只从公开简介/公开落地页提取，国家和语言不可靠时保持未知。
- 飞书新Base v3字段读取在当前bot身份缺`base:field:read`，但同一任务使用既有user身份已正常读到真实schema；这是身份/范围差异，不是需要Frankie新增权限。
- 受控导入后台摘要必须由飞书事实表复核：本轮独立扫描4,731条邮件草稿，3名作者命中0，确认“任务报告草稿0”不是自我证明。
- `source_job_id`只能追踪请求，不能证明作者有竞品合作内容；受控导入必须从当前活动已校验的证据快照重新连接帖子。调用方自带`evidence_posts`不得参与语义闸。
- YouTube的`/@handle`与`/channel/UC...`只有在历史别名唯一且实时About页频道ID一致时才能归并；仅凭URL长得相似或handle文本相同不够。
- X公开资料富化必须把所有近期帖语言计入分母；只统计en/de/es会把少量英语误判为主语言。城市、州、网站域名和时区继续不得用于猜国家。
