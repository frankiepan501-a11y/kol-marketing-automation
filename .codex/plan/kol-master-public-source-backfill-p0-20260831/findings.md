# 发现记录

## 2026-08-31

- 当前云端仓库已有 `app/kol_email_sources.py`：可读取主表现有聚合页/其他链接、YouTube About 外链、Instagram/TikTok 明确 bio link，并受公开地址、跳转次数、响应大小和 SSRF（防止访问内网）限制。
- `app/kol_email_repair.py` 已有严格邮箱闸：只有 Snov 精确返回 `valid`、无其他 KOL 占用、写前字段未变化时，才可写 `邮箱` 与 `邮箱验真状态=有效`，并逐条回读。
- 当前云端服务中，普通流程没有通用的 `T_KOL` 新建入口；明确创建 KOL 主表记录的入口是 `app/launch_evidence_author_import.py` 的受控竞品作者导入。该入口尚未保存公开官网/聚合页。
- `app/relabel.py` 只刷新现有 KOL 的内容画像，不创建 KOL；它是现有记录补充公开链接的可选接入点，但不能代替本地 SNS 爬虫的新建入口。
- 下一步必须先读取生产主表真实字段 schema，并继续定位本地 daemon 仓库的主表创建点，避免只改云端而漏掉大多数新 KOL 来源。
- 生产 schema 证明主表没有独立“官网/网站”字段：公开官网应写入现有文本字段 `其他链接`；Linktree/Beacons 等聚合页应写入现有网址字段 `聚合页URL`。
- 主表 9,528 条中邮箱为空 6,243 条，说明邮箱供给缺口真实存在；但已填邮箱的 3,187 条仍是“未验”，不能把“已有字符串”当作可发送邮箱。
- 本地 daemon 的真实创建点是 `scripts/yt_mvp.py::build_fields` → `batch_create_with_isolation`。P0 应在这里保存新发现的公开来源；邮箱有效性仍交给云端 `kol_email_repair`，保持一个严格判定入口。
- 本次主表现状试跑采用 YouTube / Instagram / TikTok 轮流取样，共 30 条；公开来源补充只要求主链接存在且至少一个公开来源字段为空。邮箱补充阶段会独立跳过已有邮箱，只对空邮箱记录工作。
- 30 条生产试跑证明公开落地页是有效补充来源：12 条可补 `聚合页URL` 或 `其他链接`；其中 3 个公开邮箱通过严格第三方验真并写入。
- 公开页面发现到的邮箱不能直接使用：本批有 5 个已归属其他 KOL 的重复邮箱、1 个验真未知、1 个验真非有效，均被系统阻止写入。
- 飞书主表的单条读取在高并发时会间歇返回 `1254607 数据未就绪`；把并发降至 2 后，正式回填无记录处理错误。后续批次应继续保持低并发、分批和逐条回读。
- 小批量生产写入采用失败即停：批量读取存在错误时零写入；写入中若发现字段被其他人修改或回读不一致，停止剩余公开来源写入，并且不继续邮箱阶段。
- 2026-09-01 邮箱阶段已改为一次列表读取和本地索引，10/30/50 条生产只读均无 `1254607` 或 `processing_error`；写前重读、写后回读和重复邮箱拦截仍保留。
