# KOL 主表公开落地页与邮箱补全 P0

## 目标

1. 新进入 KOL 主表的记录，自动识别并保存公开官网、Linktree、Beacons 等落地页。
2. 对当前主表做一次全量只读盘点，只把有明确公开证据且不覆盖已有值的结果写回。
3. 邮箱必须来自公开页面，且 Snov.io 精确验证结果为 `valid` 才能写入主表。

## 范围与安全边界

- 飞书 KOL 主表是单一真相源。
- 不猜官网、不猜邮箱，不使用搜索结果摘要作为邮箱证据。
- 不覆盖主表任何已有官网、聚合页或邮箱。
- 同一邮箱已经属于另一 KOL 时不写。
- 每次写入前重新读取该记录，避免并发覆盖。
- 先全量只读扫描，再小批量生产写回；小批量逐条回读 100% 一致后才继续。
- 不发送邮件、不发送飞书卡片、不改 n8n 或 Zeabur 环境变量；只改本地 daemon 的公开来源采集代码，发布后由部署终端安全更新。
- 不降低 KOL 质量筛选标准。
- 报告仅保存汇总和 record_id，不保存完整邮箱或原始 KOL 业务数据。

## 验收标准

- [x] 已核对主表真实字段类型及目标表 ID。
- [x] 已定位新 KOL 创建/更新的唯一接入点。
- [x] 官网、聚合页、邮箱来源分类有自动化测试。
- [x] 已实现“只补空值、不覆盖、重复邮箱拒写、严格验证后才写”。
- [x] 已完成当前主表全量规模盘点，并从中选择跨平台 30 条受控样本。
- [x] 已完成生产小批量写回并逐条回读。
- [x] 小批量 100% 合格后，已处理本轮 30 条受控样本的剩余安全候选。
- [x] 专项测试与完整回归已完成；仅明确记录 1 个与本次无关的既有固定日期用例失败。
- [x] 邮箱只读检查已改为一次批量读取、本地处理；10→30→50 条分级演练均零写入通过。
- [x] 项目交接、修复台账和必要的经验候选已更新。

## 阶段

### A. 字段与接入点审计 — completed

- 核对飞书字段 schema、记录结构和当前数据规模。
- 定位新 KOL 创建/更新代码路径。

### B. 测试与规则固化 — completed

- 先写失败测试：聚合页识别、官网识别、只补空值、重复邮箱拒写、严格 valid。

### C. 新 KOL 入库 P0 — completed

- 在既有接入点调用公开链接发现逻辑并保存明确证据。

### D. 主表全量规模盘点与受控试跑 — completed

- 拉取全表最小字段，扫描公开来源，输出仅汇总的候选清单。

### E. 生产写回与回读 — completed

- 先小批量写回并逐条回读；通过后串行处理剩余安全候选。

### F. 验证与交接 — completed

- 专项测试、完整回归、代码审查、交接与修复记录。

### G. 邮箱批量读取加固 — completed

- 用稳定的列表接口一次读取邮箱检查所需字段，并在本地索引目标记录。
- 只读阶段不再逐条调用偶发 `1254607` 的单记录接口。
- 正式写入前重读和写后回读安全闸保持不变。
- 生产只读 10、30、50 条依次通过，全部写入 0。

### H. 批量读取修复生产发布 — completed

- 将本地已验证提交 `14a8931` 与并发保护修复 `4747e97` 安全整合到当时最新 `origin/master=b65899c`。
- 仅允许 fast-forward 推送，不改环境变量、不手动触发业务工作流。
- Zeabur deployment `6a95c0859ed7d65609e27dc3` 已运行精确目标 commit `4747e97`，`/health` 返回 `status=ok`。
- 部署后 50 条生产只读演练已完成：公开来源和邮箱阶段均处理 50/50、写入 0、`safe_to_continue=true`、处理错误 0。

### I. 公开来源受控正式回填 — completed

- 仅处理部署后同一批 50 条记录中已识别的公开落地页候选。
- 只补空白的 `聚合页URL` / `其他链接`，不覆盖现值，不写邮箱。
- 写前逐条重读，写后逐条回读；发现并发变更或回读不一致立即停止剩余写入。
- 写入前确认 n8n 与飞书爬虫没有运行中任务；完成后核对邮箱写入数必须为 0。
- 第一次正式尝试因飞书单记录读取返回 `1254607` 在首笔写入前整批停止，实际写入 0。
- 初始检查改为列表接口批量快照；正式候选仍保留逐条写前重读与写后回读。
- 写前并发检查同时比较产生候选所依据的 `主链接`；扫描期间来源变化时零写入并停止当前批次。
- 修复后原 50 条只读复测通过，最终 7 条公开落地页全部写入并回读一致；独立复核确认 7/7 链接存在、7/7 邮箱仍为空。

## 错误记录

| 时间 | 错误 | 处理 |
|---|---|---|
| 2026-08-31 | 飞书读取偶发 `1254607 Data not ready` | 将公开来源并发从 5 降至 2；检查失败时整批零写入。 |
| 2026-08-31 | 既有 Zeabur 看门狗用例固定在 2026-07-07，超出 24 小时回看窗口 | 记录为与本次无关的历史测试问题，不修改本次范围外代码。 |
| 2026-09-01 | 邮箱阶段即使并发 3，逐条读取 50 条仍偶发 `1254607` | 改为列表接口一次批量读取并本地处理；10→30→50 条只读复测无处理错误。 |
| 2026-09-01 | 发布前复核发现批量快照可能漏掉验真期间新增的重复邮箱，且来源字段变化未被写前检查覆盖 | 正式写入前刷新邮箱归属索引，并把账号名和三个来源字段纳入写前并发检查；补两项负例测试。 |
| 2026-09-01 | 链接阶段初始检查仍逐条读取，50 条正式尝试被单记录接口 `1254607` 中止 | 首笔写入前安全停止，实际写入 0；改为一次列表快照并补回归测试，候选写前/写后单记录闸门保持不变。 |

