# 进度记录

## 2026-08-31

- 用户明确授权执行 P0，并要求对当前 KOL 主表试跑官网、Linktree、Beacons 与公开邮箱补全。
- 已确认执行顺序：先核对字段和写入点，再全量只读扫描，小批量生产写回，最后逐条回读。
- 阶段 A 已完成：生产主表共 9,528 条；字段类型已核对。`聚合页URL` 是网址字段、覆盖 481 条；`其他链接` 是文本字段、覆盖 891 条；不存在单独的“官网/网站”字段；邮箱覆盖 3,125 条、空 6,243 条。
- 已定位通用新 KOL 创建入口：陈翔宇部署机仓库 `kol-scraper-daemon/scripts/yt_mvp.py`。旧逻辑只保留社交平台类别，没有把官网与 Linktree/Beacons URL 写进主表，且抓到的邮箱只标“未验”。
- 阶段 B 已完成：新增公开 URL 分类、发现、空字段写入计划、跨平台试跑选择测试；云端专项与回归测试通过，本地 daemon 公开来源测试通过。
- 阶段 C 已完成代码改动：本地 daemon 新建 KOL 时可从 YouTube 公开外链保存 `聚合页URL`、`其他链接` 与其他社交平台；不猜 URL，不在本地把邮箱判为有效。
- 阶段 D 已完成：同一批 30 条跨平台记录先做两轮只读演练，再做一次受控生产回填。
- 生产结果：公开来源字段写入并回读成功 12 条；严格验真有效邮箱写入并回读成功 3 条；重复邮箱 5 条、验真未知 1 条、验真非有效 1 条均被拦截；没有覆盖原值，也没有回读不一致。
- 飞书大表并发读取曾返回 `1254607 数据未就绪`；已把公开来源回填并发从 5 降到 2。正式回填 30 条全部处理完成，无 `processing_error`。
- 复审加固：已有邮箱立即跳过，不再验真或计入新增；公开来源读取失败时整批零写入，出现并发变更或回读不一致时立即停止剩余写入，并跳过邮箱阶段。
- 与最新 `origin/master=23e8c23` 安全整合后，云端专项测试 33 项通过、5 个子测试通过；完整回归 914 项通过、31 个子测试通过，仅 1 个既有 Zeabur 固定日期看门狗用例失败。本地 daemon 30 项测试全部通过。
- 最新主分支已移除不可用的独立 Snov 验证端点；本次按当前生产能力改为 `Snov find_email` 返回的有效邮箱必须与公开页面邮箱完全一致才允许写入。匹配不到就保持空值，不降级。
- 当前阶段：E（最终复审、提交、部署与交接记录）。
- 发布后的扩展只读演练已完成：通用跨平台样本 100 条，公开来源可补 41 条、无新增 59 条；邮箱子样本 50 条中已有邮箱 37 条、公开页无邮箱 12 条、验真暂不可用 1 条；全部 `writes=0`。
- 空邮箱定向盘点：9,528 条中有 188 条“邮箱为空且已有聚合页/其他公开链接”；均衡抽取 50 条（Instagram 13、TikTok 13、YouTube 12、X 12）只读演练。
- 空邮箱定向 50 条结果：公开来源无新增 43、可补 7；邮箱公开页无邮箱 39、验真暂不可用 10、飞书逐条读取失败 1；因检查错误自动判定 `safe_to_continue=false`，实际写入 0。
- 同批 50 条纯回读复核：48 条成功、2 条仍被飞书 `1254607 Data not ready` 拦截，确认较大逐条批次当前不稳定，不继续扩到全部 188 条。
- 前 10 条小批量复测：公开页无邮箱 7、验真暂不可用 3、处理错误 0、写入 0，`safe_to_continue=true`。当前只读证据支持以 10 条为安全起始批次，不支持直接全量扩跑。
- 当前 P0 唯一外部阻塞仍是陈翔宇部署终端截图：需确认 `0a03fc4`、1 个逻辑 daemon、最新启动后无 Traceback；等待期间未重发、未催促。

## 2026-09-01

- 邮箱检查已从“每条调用一次 `get_record`”改为“列表接口一次读取所需字段，再按 record_id 在本地处理”；重复邮箱所有者索引复用同一批量快照。
- 正式写入的两层保护没有取消：每条仍会在写前重新读取，写后再次回读；本次仅运行 dry-run（只演练不写入）。
- 新增回归测试证明：批量列表可用、单条读取接口模拟 `1254607` 故障时，只读检查仍完成且单条接口调用数为 0。
- 相关回归：34 项通过、5 个子测试通过。完整回归：915 项通过、31 个子测试通过；仅原有固定日期 Zeabur 看门狗用例失败。
- 生产只读演练逐级通过：10/10、30/30、50/50；三档 `writes=0`、`safe_to_continue=true`、`processing_error=0`。
- 最终 50 条结果：40 条没有公开邮箱，10 条验真服务暂不可用；公开来源侧另识别 7 条可补链接，但本次没有写入。
- 证据：`C:/tmp/kol-empty-email-public-source-bulk-10-20260901-evidence.json`、`C:/tmp/kol-empty-email-public-source-bulk-30-20260901-evidence.json`、`C:/tmp/kol-empty-email-public-source-bulk-50-20260901-evidence.json`。
- 本次未部署；陈翔宇部署终端截图仍是独立的既有发布验收项。
- 发布前双轴复核发现两个并发保护缺口：外部验真期间另一 KOL 可能新增同一邮箱；运营可能修改账号名或公开来源字段。已先补失败测试，再做最小修复。
- 正式写入现在会在全部公开来源检查和验真结束后、首笔写入前重新批量读取邮箱归属；每条写入前同时核对邮箱、验真状态、账号名、主链接、聚合页 URL 与其他链接。只读阶段仍只批量读取一次。
- 修复后邮箱专项 14 项通过；公开来源相关回归 36 项及 5 个子测试通过；完整回归 917 项及 31 个子测试通过，唯一失败仍是既有固定日期 Zeabur 看门狗用例。
- 最终双轴代码复核无阻塞问题；发布前只读检查确认 `uvBfJBtGH93FPa6w active=true`、n8n 全局运行中 execution=0、飞书爬虫运行中=0，两项活动后台任务均非 `running`。
- 仅 fast-forward 推送 `b65899c..4747e97` 到 `origin/master`；未强推、未改环境变量、未启停或改写 n8n，也未触发补池、爬虫、卡片或邮件。
- Zeabur 自动部署 `6a95c0859ed7d65609e27dc3` 已运行精确 commit `4747e97b82a40b2a4072b1ab7d789984b545e38c`；`https://kol-auto.zeabur.app/health` 返回 `status=ok`。
- 部署后同一批 50 条生产只读演练通过：公开来源 43 条无新增、7 条可补；邮箱 40 条无公开邮箱、10 条验真暂不可用；两个阶段均 `processed=50`、`writes=0`、`safe_to_continue=true`、处理错误 0。
- 部署后证据：`C:/tmp/kol-empty-email-public-source-postdeploy-50-20260901-evidence.json`。本轮没有获得正式主表批量写回授权，因此没有写入生产表。
- 获得公开来源小批量正式回填授权后，先等 n8n 运行中 execution 和飞书爬虫任务自然排空；没有停止任务、没有手动触发业务流程。
- 第一次 50 条 `commit-links` 在首笔写入前被飞书单记录读取 `1254607 Data not ready` 拦截：公开来源写入 0、邮箱写入 0。证据：`C:/tmp/kol-empty-email-public-source-commit-links-50-20260901-evidence.json`。
- 根因是链接阶段初始检查仍逐条调用单记录接口；已改为列表接口一次读取最小字段并按 record_id 本地索引。正式候选的逐条写前重读和写后回读没有取消。
- 新增 2 项回归：初始检查不得调用单记录接口；正式写回必须保留逐条写前检查和写后回读。公开来源专项 7 项通过，公开来源与邮箱相关回归共 23 项通过。
- 原 50 条修复后只读复测通过：43 条无新增、7 条可补，公开来源和邮箱阶段均写入 0、无处理错误。证据：`C:/tmp/kol-empty-email-public-source-postfix-dryrun-50-20260901-evidence.json`。
- 最终只对 7 条已验证候选执行正式链接回填：7 条全部 `written_public_source`，逐条写后回读一致；邮箱阶段保持 dry-run，邮箱写入 0。证据：`C:/tmp/kol-empty-email-public-source-commit-links-7-20260901-evidence.json`。
- 独立只读复核：7/7 记录可读、7/7 已有 `聚合页URL` 或 `其他链接`、7/7 邮箱仍为空。未覆盖任何已有链接或邮箱。
- 发布前差异复核发现：公开来源候选依赖 `主链接`，但写前检查原先只比较两个目标字段。已先用负例复现，再把 `主链接` 纳入写前并发比较；来源变化时写入 0 并停止该批。
- 最终验证：相关回归 24 项通过；完整回归 920 项及 31 个子测试通过，唯一失败仍是与本次无关的既有固定日期 Zeabur 看门狗用例。
