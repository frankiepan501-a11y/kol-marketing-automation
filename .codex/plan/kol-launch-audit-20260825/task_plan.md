# KOL 集中宣发双活动审计计划（2026-08-25）

## 目标

先完成 FUNLAB Dave 与 POWKONG 食人花二代两项集中宣发任务的真实进度审计，再按授权修复三个 P0：后台任务状态可回读、食人花确定性自动补池、站外候选完整来源回填。

## 安全边界

- 保持 n8n 工作流 `uvBfJBtGH93FPa6w` active，不启停、不改筛选标准。
- 不手动触发补池、草稿或发信任务；验收只等自然定时轮次。
- 不发送任何 KOL 邮件或运营群卡片；部署终端更新只通过已授权的聪哥分身1号私聊陈翔宇。
- 先离线 TDD；所有测试通过、在途任务排空后只做一次云端生产发布。
- 不输出 KOL 邮箱、姓名等个人明细。

## 验收标准

1. 两项活动分别给出截至审计时点的关键漏斗数字。
2. 每个异常明确归属：程序故障、调度故障、供给不足、资料/邮箱不足、审核积压、发送额度或结果闭环缺失。
3. 每个 P0 问题写清影响、根因、最小整改动作和修复后验证方法。
4. 区分“系统正常但产出低”和“系统真正卡死”，不以 n8n success 代替业务成功。

## 步骤

- [x] 读取当前项目交接、生产边界和历史验收。
- [x] 建立只读检查基线并确认生产版本/工作流状态。
- [x] 回读两活动最新自然 execution 与后台 job。
- [x] 读取活动漏斗与品牌邮箱滚动额度。
- [x] 归因卡点并形成整改清单。
- [x] 更新审计记录并完成只读报告；生产整改等待 Frankie 授权。
- [x] P0-1：修复长结果截断任务标记，保证 `/launch/runtime/jobs/latest` 可稳定回读。
- [x] P0-2：食人花接入通用七层词库的 7 天复用和来源健康规则；AI 额度耗尽不阻塞。
- [x] P0-3：站外新建和已存在 KOL 均回填活动 ID、任务 record_id、词源和发现词。
- [x] 目标测试与全量回归通过，完成代码审查并修复三项审查缺口。
- [x] 部署终端验收通过：版本 `fd35e0e`、一个逻辑 daemon（父子两个 Python 进程）、最新启动后无 Traceback。
- [x] 等待在途任务排空后只发布一次；fast-forward 到 `c5aa6a9`，Zeabur 目标部署运行且健康接口正常。
- [x] 完成两活动自然轮次验收：部署终端已更新为 `58375b0` 且保持一个逻辑 daemon；食人花 16:50 自然轮次用确定性词创建真实国家任务，daemon 自然领取并成功完成，质量门槛未降低；Dave 17:00 自然轮次完成并明确归因为供给不足。最终 n8n 与飞书爬虫运行中均为 0。
- [x] P1 重新审计：按原审计口径回读 Dave/食人花最新漏斗、待审/缺画像、回复待处理、承诺/寄样/上稿以及 Launch latest，先确认旧 P1 哪些仍存在。
- [x] P1-可观测性：为当前进程内 `/launch/runtime/jobs/latest` 返回真实 job_id 建立失败测试并做最小修复。
- [x] P1-候选转化：只处理“缺画像自动回资料刷新、系统建议通过直达草稿、仅边界项人审”仍缺失的部分，不降低国家/语言/品类/粉丝/邮箱质量规则。
- [x] P1-回复闭环：只处理“感兴趣/谈条件/需要样品/已同意日期”到明确下一动作和承诺日期、以及日报分阶段展示仍缺失的部分，不自动对外发送。
- [x] 运行专项与完整回归，按本次固定点做双轴代码审查；只在生产保护闸通过后 fast-forward 发布云端。专项 126 项 + 3 个子测试通过；完整回归 799 项 + 23 个子测试通过，仅 1 个既有固定日期看门狗失败；`d4d440f` 已仅 fast-forward 发布，Zeabur 目标部署运行且 `/health=ok`。
- [x] 等自然轮次只读验证 P1 结果，更新交接与修复台账；除非本地 daemon 代码确有改动，否则不要求陈翔宇操作。食人花自然轮次已结束并确认 `quality_filters_lowered=false`；Dave 19:00 自然轮次已产生真实 job_id，候选矛盾项清零、分阶段日报口径回读通过。该 job 尚在后台排队运行，作为业务供给任务继续自然执行，不再把“等待业务任务结束”混同为 P1 技术未完成。
