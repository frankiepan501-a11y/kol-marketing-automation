# AMZ Procurement Card Callback Fix

## Goal
修复德国站采购成本回填卡片的真实回调问题，并把发卡前自测固化到代码，覆盖按钮、输入框、确认按钮、写表和原卡 PATCH。

## Phases

| Phase | Status | Success Check |
|---|---|---|
| 1. 补写用户已填的第三个产品 | complete | `B0D1CLBFD9` 候选表读回成本 12.5 和 1688 链接，原卡显示 3/4 已回填 |
| 2. 修复表单回调解析 | complete | 单测覆盖飞书嵌套/列表/标准表单返回结构 |
| 3. 增加卡片发前自测 | complete | 本地脚本检查三个 URL 按钮、输入框、form_submit payload 和模拟回调 |
| 4. 验证、提交、部署 | complete | 单测和自测通过，commit 推送 master，Zeabur 健康检查通过 |
| 5. 沉淀教训 | complete | 写入 memory-candidate，后续卡片必须先自测再发 |
| 6. 进入50件验证节点 | superseded | 2026-07-24 复盘确认该节点位置错误：当前应是选品阶段的“小批量采购建议”，真正验证应后移到 Listing 上架和库存可售之后 |
| 7. 选品阶段收尾重构 | complete | 已将四大阶段、选品收尾任务、采购建议与上架后验证边界沉淀到项目记录 |
| 8. 选品结果确认卡 P0 | complete | 本地完成模块、接口、回调路由、自测和交接文档；2026-07-28 已补 Sorftime 五站月销快照，卡片改为分开展示样本月销、产品级竞品月销、类目新品月销、样本数和数据质量 |
| 9. 采购阶段预览卡 B 路径 | complete | 已新增只读 Frankie-only 预览卡并真实发送；确认采购阶段派发口径后再正式发采购部 |
| 10. 采购部执行卡发送 | complete | Frankie 确认口径 OK 后，已发送采购部执行版卡给 2 名采购专员；只含 Go/条件推进 4 个可执行产品，不含暂缓/淘汰产品 |

---

# 2026-08-19 戴夫活动：NYXI 全证据补接、3 人补位与 G4 测试邮箱验证

## Goal

把 NYXI 非官方帖子完整接入本次 Dave 活动排序，执行人工排除后的 3 人自动补位，并完成测试邮箱 raw 内容验证；不向真实 KOL 发信。

## Scope and gates

- 只影响活动 `launch-20260915-funlab-dave-ys11-5`。
- 复用现有竞品帖子表、活动参与记录和 Zoho 测试端点，不新建第二套发送器。
- `EMAIL_DRY_RUN_TO` 必须开启且仅允许测试邮箱；真实 `发送邮件授权` 继续关闭。
- 人工排除先出池，再从符合活动国家/语言和全局触达规则的新 cold 候选中补 3 人。

## Phases

| Phase | Status | Success Check |
|---|---|---|
| 1. 恢复上下文与只读生产快照 | complete | 回读活动、参与记录、竞品帖子、草稿与功能开关；确认无真实发送授权 |
| 2. 实现全证据覆盖与身份匹配报告 | complete | 非官方证据 2,988 条、作者 1,457 位已以 50 个只读快照节点接入；后台预览从快照完成排序 |
| 3. 执行 3 人自动补位 | complete | 3 条人工排除退出当前名单；补入 3 名新开发对象，当前有效名单恢复 20 人 |
| 4. G4 测试邮箱验证 | complete | FUNLAB 测试邮件仅发测试邮箱；raw 主题、正文、产品名、链接、占位符、发件账号 9 项全部通过 |
| 5. 生产回读、测试、审查与交接 | complete | 专项/全仓测试、生产授权回读、独立代码审查和交接文档完成；真实发送保持关闭 |

---

# 2026-08-20 食人花候选质量 P1：自动资料维护与待审核补池

## Goal

把 P0 新增的 KOL 画像字段接入现有 YouTube 重打标签流程，并从新规则筛出的对象中补充一批食人花候选；所有新补候选强制进入待审核，不因活动当前已开启邮件授权而自动生成草稿或发送。

## Scope and gates

- 只扩展 `launch-20260915-powkong-piranha-v2` 的待审核候选，不改变 Dave 活动。
- 自动维护 `内容垂类 / 主机生态 / 最近发布日 / 近90天发布数 / 资料可用状态 / 资料核实时间 / 触达路由状态`。
- 新候选必须通过国家、语言、粉丝、Switch/Nintendo 内容、活跃度、邮箱和全局重复触达检查。
- 新补记录一律 `审核结论=待审核`，本阶段不创建邮件草稿、不触发 Zoho。

## Phases

| Phase | Status | Success Check |
|---|---|---|
| 1. 现状与字段口径回读 | complete | 已回读活动授权、20 条旧审核记录、KOL 主表字段类型和目标市场；确认活动邮件授权=true |
| 2. 自动资料维护实现 | complete | 既有重打标签流程与新后台定向入口均写全 P0 画像字段；单条失败可见且不拖垮整批 |
| 3. 待审核补池安全入口 | complete | 新候选强制待审核，证据模式校验闭合，代码路径不创建草稿、不调用发送器 |
| 4. 小样本与生产扩充 | complete | 新发现129名对象后跑全池后台预览；3名合格新对象进入食人花专属审核视图，全部待审核，草稿0、外发0 |
| 5. 测试、部署、审查与交接 | complete | 活动粉丝范围、内容集中度和旧误判标签清理均已测试部署；生产参与记录与主表完成回读 |

## Errors Encountered

| Error | Attempt | Resolution |
|---|---|---|
| `memory.py precheck` 不接受 `--session` | 首次预检附带 session 参数 | 去掉不支持参数后重跑；预检建议大任务先持久化计划，因此继续以本计划文件做上下文收口 |
| 单个飞书关联字段写入 2,988 条记录返回 `1254001 WrongRequestBody` | 全量关系单元格写入 | 改为 50 个有序、可校验、禁止外部动作的证据快照节点；全部回读成功后才原子切换活动版本 |

## Decisions

- 不用真实用户继续点卡验证；本轮先用本地模拟回调和结构化自测覆盖可控问题。
- 不创建测试脏记录写生产候选表，避免污染采购数据。
- 2026-07-24 修订：亚马逊欧洲铺货/精铺工作流拆成四个大阶段：选品阶段、采购阶段、Listing 上架阶段、运营与监控阶段。
- 2026-07-24 修订：当前仍在选品阶段收尾，不应把下一步称为“50件验证”。当前输出应是“选品结果确认 + 小批量采购建议”，由运营/上司确认后才触发采购阶段。
- 2026-07-24 修订：真正验证节点放在采购、到货、Listing 上架之后，用 7/14/30 天销量、转化、退货、适配投诉和评价表现判断扩采/改款/淘汰。
- 2026-07-24 修订：选品结果确认卡的四个按钮是业务决策按钮：`Go` 进入采购确认，`条件推进` 进入采购复核，`暂缓` 补数据/重算，`淘汰` 归档；不能在此节点直接下单或启动上架验证。
- 2026-07-28 修订：选品确认卡不得把“竞品月销”写成单一黑箱字段；必须区分样本 ASIN 月销、产品级竞品月销、类目新品月销、样本数和数据质量，缺 exact ASIN 月销时明确标注不可用。
- 2026-07-29 修订：选品结果全部确认后，先发 Frankie-only “采购阶段预览卡”复核采购部交接口径；预览卡只读，不写采购阶段触发表、不发采购部、不放会误触发流程的回调按钮。
- 2026-07-29 修订：正式采购部执行卡不能沿用“给 Frankie 确认”的预览文案；采购部版本必须显示“采购阶段执行清单 / 采购部执行”，且需要 `procurement_approved=true` 才能越过 Frankie-only 安全闸。
- 2026-08-19：高基数竞品证据不再塞进单个关联单元格；活动主表保留代表样本，完整证据放有序快照节点并用数量、连续分片和 SHA-256 校验。
- 2026-08-19：历史已取消参与记录继续用于防止重复选人，但不计入本次当前名单的流出人数；名单闸门不得覆盖证据排序版本。
- 2026-08-20：活动候选粉丝范围以活动表 `KOL粉丝下限/上限` 为准；未配置的旧活动才回退产品价格带，避免活动价与产品库报价不一致时错杀候选。
- 2026-08-20：机器画像至少需要3条近期游戏/主机/硬件内容证据；一两条搜索词碰巧命中的频道不得写成 Nintendo/Switch 画像。
- 2026-08-20：只有“本轮近期标题明确证明旧机器标签误判”且没有近期人工核实保护时，系统才清空旧机器内容标签；产品特定排除仍只写活动参与记录。

## 2026-08-20 IndieAlpaca 首封真实灰度

| Phase | Status | Success Check |
|---|---|---|
| 6.1 发送前生产回读 | complete | 参与记录已人工通过；主页、联系人、邮箱、产品、历史触达和活动版本全部一致 |
| 6.2 只生成并发送一封 | complete | 仅 IndieAlpaca 生成草稿 `recvsLHSKGv4tc` 并发送 1 封；同 nonce 禁止补发 |
| 6.3 发后 raw 与业务回读 | complete | Zoho raw 9/9 通过；草稿、跟进、KOL 状态、参与记录与活动锁回读一致 |
| 6.4 收口 | complete | 活动发送闸=false；代码、测试、部署和审计备注完成 |

授权边界：Frankie 已明确授权 `https://www.youtube.com/@IndieAlpaca` 作为 Dave 活动首名真实 KOL 灰度对象；只覆盖这一名、这一封 cold 开发信，不覆盖其他参与者、follow-up、寄样、付费或批量发送。

## 2026-08-20 两活动当日批量放量准备度审计

目标：不再等待 IndieAlpaca 的观察期，核对 Dave 与食人花两项活动今天开始批量放量还缺哪些生产条件；本阶段只读审计，不执行批量真实发送。

| Phase | Status | Success Check |
|---|---|---|
| 7.1 回读两项活动、产品锁和名单 | completed | Dave 20/20 已审核通过、1 已发送、19 未发送；食人花活动缺产品绑定/范围/正式名单；两款产品锁均为活动专用 |
| 7.2 回读邮箱与发送服务余量 | completed | 服务健康、无暂停、无 dry-run；Zoho 最近24小时约 FUNLAB 13 / POWKONG 4，系统日上限均为120 |
| 7.3 审计批量释放能力 | completed | 现有活动流程只有同步单人端点；缺后台批次、状态查询、批次限速和大名单续跑，不能直接把427封一次性释放 |
| 7.4 给出今天最小放量方案 | completed | 建议先以每品牌每批6封分时启动；Dave 先发剩余19，食人花补齐活动配置/名单后同日启动首批，同时扩展完整427封漏斗 |

## 2026-08-20 两活动自适应正常放量

目标：把“正常放量”定义为在邮箱健康和全局防重复前提下，尽量用满 FUNLAB、POWKONG 两个 Zoho 品牌邮箱的每日安全额度；开发池按回复率、承诺缺口和上稿进度动态扩展，不把 427 人当作硬上限。

| Phase | Priority | Status | Success Check |
|---|---|---|---|
| 8.1 活动价 / KOL Deals 定价待办 | P1 | pending | 分别确认食人花、Dave 的公开活动价、KOL 专属优惠、有效期、地区/渠道、佣金兼容与最低毛利；未确认前首封冷邮件不写价格，报价类回复继续人审 |
| 8.2 活动队列接入现有唯一发送器 | P0 | complete | 活动队列、Zoho 24h 权威计数、Raw 证书和后台发送状态查询已部署；10分钟定时器已切换为后台受理模式 |
| 8.3 Dave 当日启动 | P0 | complete | 已建立142条参与记录、120条活动草稿；四批各6封均成功，新增批量发送24封，加上IndieAlpaca灰度共25封；邮箱粘连缺陷已修复并恢复10分钟自动放量 |
| 8.4 食人花当日启动 | P0 | complete | 20名候选人工审核完成（1通过、18排除、1待补资料）；仅通过的 DSimphony 生成并发送活动开发信，活动正式运行且持续受统一发送中心额度、活动锁和Raw证书控制 |
| 8.5 每日反馈控制器 | P0 | complete | n8n `VWTMNkXf0zcs4Kvz` 已启用，每天北京时间17:00依次检查Dave和食人花；只使用活动内已发/回复/承诺数据 |
| 8.6 按产品审核视图与字段顺序 | P1 | complete | Dave 与食人花各有独立人工审核视图；首屏只放判断证据、对象、地区语言、粉丝和审核结论，Grid View 不变 |

### Confirmed operating decisions

- `427` 是中性情景下的首轮测算基线，不是开发池上限。
- 每品牌 Zoho 邮箱滚动24小时硬上限暂按 `120` 封总外发；活动 cold 邮件只能使用扣除回复、报价、寄样和其他业务邮件后的剩余额度。
- 日常目标为使用 90%–100% 安全额度；现有发送器会按当前待发 reply/ship/quote 数量动态预留，最多 30 封；没有时间敏感邮件时自动把额度释放给 cold。
- 每品牌每批 6 封，分时释放；禁止为了“用满额度”一次性突发 60+ 相似邮件。
- 新开发池维持至少未来两日的可发送缓冲；当队列低于两日额度或承诺/上稿进度落后时自动扩池。
- 价格待办不阻塞首封冷开发，但在价格、Deals 和最低毛利未确认前，系统不得自动承诺折扣、付费合作或最终报价。

### Minimum start checklist

1. 给活动草稿增加可验证的活动身份，并允许它在通过活动名单/产品锁/全局重复触达检查后进入现有 `auto_send`；普通 cold 仍被活动专用锁拦截。
2. 统一以 Zoho 发件箱滚动24小时真实发送量控制120封上限，避免手工发送或其他流程尚未同步到草稿表时超额。
3. Dave 把剩余19名已审核对象生成活动草稿，并建立追加式候选补位，不再使用“整批替换、最多100人”的名单锁接口承载400+开发漏斗。
4. 食人花补齐产品主记录ID、国家/语言、竞品证据模式（可选“不引用”）、名单版本和首批合格候选。
5. 两品牌分别做一次“只生成不发送”批次回放；确认活动锁、重复触达、邮箱、产品英文名、链接、占位符和额度均通过后，才打开活动 cold 放量。

## 2026-08-20 P0：两活动无人值守自治补池

目标：Dave 与食人花活动不再依赖 Frankie 每轮打开 Codex 手工触发；系统按小时根据滚动 24 小时邮箱余量、活动承诺缺口和可发送草稿库存自动补池。确定性规则合格者直接进入活动队列；资料缺失或语义边界项只进入 KOL 运营审核，不交 Frankie 逐条判断。

| Phase | Status | Success Check |
|---|---|---|
| 9.1 固定接口与安全边界 | complete | 自治入口只补池/建草稿，不直接发信；停止/保持继续以活动承诺和预计按时上稿为准 |
| 9.2 测试先行与代码实现 | complete | 覆盖库存缺口、主表补池、资料刷新、外部拓新、运营审核分流和任务状态持久化 |
| 9.3 生产表与 n8n 调度 | complete | 活动表持久化后台状态；启动流 `uvBfJBtGH93FPa6w` 每小时00/05分错峰受理，审计流 `ijIcjoYO9Jm1Vdkw` 每小时50分独立核验最新持久状态，避免长连接和网关超时 |
| 9.4 部署与生产小样本 | complete | commit `9a5a41f` + `912756c` 已部署；Dave与食人花真实后台任务均成功，定时启动 execution `968537`、独立完成审计 execution `968515` 均成功 |

### P0 implementation seams

1. `launch_runtime.autonomous_refill(...)`：确定性控制器，返回 `action / quota / inventory / append / refresh / discovery / review_pool / queue`。
2. `/launch/runtime/autonomous-refill` + `/launch/runtime/jobs/{job_id}`：启动后台任务并从活动表持久回读状态；HTTP 受理不等于完成。
3. n8n：短启动流只确认后台已受理；独立审计流在每小时50分读取活动表最新持久状态，只接受新鲜的 `success`，不能把 200/accepted 当完成。

### 2026-08-20 Errors Encountered

| Error | Attempt | Resolution |
|---|---|---|
| 本机 lark-cli 1.0.64 的 `base +record-get` 不支持文档中的 `--fields` | 首次裁剪字段读取 | 改为先看当前命令 help，再使用该版本支持的只读参数；不改 CLI 版本、不切 raw API 盲试 |
| 单人端点仍调用全池 `replay_candidate`，读取全表导致长等待且未建草稿 | 首次两次放行尝试 | 改为只查该联系人、该邮箱和产品家族的 `_fast_precheck`；116 项活动测试通过后部署 |
| 飞书把 `粉丝数` 返回为字符串，`{kol_sub:,}` 触发 `ValueError` | 修复后首次真实请求 | 在 `enrich._subscriber_count` 统一转为非负整数；只生成不发送验证通过后再用同 nonce 执行 |
| 已发 raw 初检 8/9，产品身份项误报 | 唯一真实发送后的 raw 检查 | 邮件实际含 `Dave the Diver controller`；校验器补充“IP + 产品类型”规则，直接读取同一 Zoho 消息重新验证为 9/9，未补发 |
| 长等待版 n8n 工作流启用后没有稳定产生 schedule execution | 首次小时调度验证 | 用最小定时流证明 n8n scheduler 正常；将架构拆成“短启动 + 独立完成审计”，更新时显式停用/重启注册，两个真实定时 execution 均成功 |
| 持久任务状态使用 `updated_ts`，审计首版只读 `started_ts` | 首次审计 execution `968505` | 审计改为优先读取 `updated_ts`、兼容内存态 `started_ts`；重跑 execution `968515` 成功 |

## 2026-08-20 食人花自治补池“假成功”P0修复与定时验证

目标：修复食人花自治任务“代码执行成功，但剩余额度很高、可发库存仍为0”仍被记为成功的问题；让后台自动承接爬虫新增候选、刷新资料、按原质量标准筛选并入唯一发送中心，同时用独立定时验证检查业务结果。

### 验收边界

- 公开执行入口：`POST /launch/runtime/autonomous-refill`，只补池、刷新、建参与记录和草稿，不直接调用 Zoho。
- 公开审计入口：`GET /launch/runtime/jobs/latest?campaign_id=...`，必须返回可判断的业务结果和安全计数。
- 当品牌仍有额度、活动动作仍为扩池、可发库存为0且本轮没有任何补给进展时，任务不得标为普通成功。
- 定时验证必须同时检查状态新鲜度、剩余额度、可发库存和本轮补池进展；不能只看 HTTP 200 或进程未崩。
- 不降低国家、语言、内容、活跃度、重复触达和历史关系规则；待补资料/人工边界项不自动发信。

| Phase | Priority | Status | Success Check |
|---|---|---|---|
| 10.1 复现并写失败测试 | P0 | complete | 公开任务结果在“余量>0+库存0+无进展”时明确为 degraded；测试先红后绿 |
| 10.2 补业务结果与补池续跑 | P0 | complete | 新发现对象在后续周期自动重进预览/刷新/筛选；已有待跑发现任务明确标记为供给进行中；合格者自动入队，边界项保留人审及稳定原因代码 |
| 10.3 定时业务验证 | P0 | complete | 云端每15分钟检查食人花最新任务；业务字段缺失、非法、过期、degraded或supply_blocked均失败，不建立依赖本机的重复自动化 |
| 10.4 测试、部署与生产回读 | P0 | complete | 聚焦测试、全仓测试、n8n校验、Zeabur部署及一轮真实后台任务回读完成 |
| 10.5 修复记录与交接 | P1 | complete | 文档记录根因、改动、验证、剩余风险和回滚边界 |

## 2026-08-21 双活动日发信与全链路只读审计

目标：按活动而不是按品牌邮箱统计当日真实开发信，并核对集中宣发、常规KOL跟进、定时调度、运营卡片和数据闭环是否完整。本阶段只读，不修改生产工作流、不发送邮件。

| Phase | Priority | Status | Success Check |
|---|---|---|---|
| 11.1 当日活动发信对账 | P0 | complete | 以活动参与记录关联草稿、已发送状态和北京时间发送日期交叉核对；Dave今日1封、食人花今日1封 |
| 11.2 运行状态与补池审计 | P0 | complete | 唯一发送中心、回复监听、富化、审核兜底、自治启动、业务结果审计和17:00反馈均启用；食人花11:17完成一轮供给刷新，Dave 11:00任务仍在后台运行 |
| 11.3 定时任务时区审计 | P0 | complete | 同时核对workflow名称、timezone、cron和真实execution时间；发现5条名称/预期与实际北京时间不一致 |
| 11.3A 食人花补池真实产出判定 | P0 | complete | 假成功已转degraded；DeepSeek 402时由受控长尾词降级继续补给；生产回放真实创建5条待触发发现任务并逐条回读 |
| 11.4 修正高风险定时频率 | P0 | in_progress | 草稿归档清理恢复为每周一04:00；上稿登记卡恢复为每周一10:30，并用真实execution验证，不只看名称 |
| 11.5 修正其他业务时点 | P1 | pending | Follow-up、寄样对账、手动发送补登记分别恢复到10:00、09:20、09:40北京时间 |
| 11.6 活动结果闭环 | P0 | pending | 回复/洽谈自动回填承诺上稿时间，上稿登记自动回填实际上稿时间和链接；否则自治控制器无法按真实目标停止扩池 |
| 11.7 活动级效果与毛利闭环 | P1 | pending | 把曝光、订单、GMV、费用和毛利归因到活动参与记录或可关联的事实表，并用于下一轮目标/预算倒推 |
| 11.8 活动价与KOL Deals | P1 | pending | 延续8.1：确认两产品活动价、专属优惠、有效期、渠道、佣金兼容和最低毛利 |
| 11.9 周报首次真实执行验收 | P1 | pending | 任务完成周报和上稿×任务进度周报虽已启用，但暂无生产execution历史；下一个周一回读真实结果 |
| 11.10 运营卡片与表治理 | P2 | pending | 每周日任务管理卡、每日17:00邮箱额度卡尚未实现；完整表结构简化和行数治理仍需继续 |
| 11.11 媒体人集中宣发分支 | P2 | pending | 当前两项试点参与记录均为KOL；媒体人沿用旧流程，但集中窗口模式尚未做生产试点 |
