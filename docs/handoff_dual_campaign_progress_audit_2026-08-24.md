# Dave＋食人花集中宣发进度审计交接（2026-08-24）

## 结论

两项活动的邮件通道和额度都正常，当前停滞原因是“可发送名单库存为0”，不是邮箱限额。Dave与食人花最近一轮自治补池都已明确返回`degraded / supply_blocked`。如果只继续等定时器，定时器会继续运行，但不会自然产生开发信。

本次为只读审计：没有修改生产表、n8n、Zeabur配置，没有生成或发送真实邮件，也没有主动发送业务飞书卡片。一次定向只读回放失败进入服务通用异常处理，可能产生1条内部异常告警；未产生业务表写入。

## 审计时点事实

| 指标 | Dave（FUNLAB） | 食人花二代（POWKONG） |
|---|---:|---:|
| 活动目标上稿 | 20 | 20 |
| 当前审核通过新开发 | 160 | 18 |
| 当前待审核/待补资料 | 20 | 20 |
| 活动累计已发开发信 | 152 | 18 |
| 本日活动开发信 | 0 | 0 |
| 累计回复 | 4 | 1 |
| 明确承诺上稿 | 0 | 0 |
| 实际上稿 | 0 | 0 |
| 当前可发送库存 | 0 | 0 |
| 品牌邮箱滚动24小时使用 | 3 / 120 | 0 / 120 |

日报后台任务`launchreport-9d429f9469d1`只读完成：`validation.ok=true / notified=false / business_writes=0 / operational_receipt_writes=0`。

## 直接影响当前进度的P0

### 1. Dave人工审核结果没有解除全局触达拦截

Dave有6条审核通过但没有生成草稿：

- 2条属于已有邮件线程，系统正确阻止再次发新冷邮件；
- 1条命中同产品近期触达，系统正确保持等待；
- 3条是本轮刚人工审核通过的竞品证据作者，但KOL主表仍是`触达路由状态=待人工核对`，因此发送预检继续拦截。

最小修复：人工审核“通过”时必须同步产生一条明确、可审计的全局路由结论；只有这3条可以解除`待人工核对`。已有线程和近期触达的3条仍保持原拦截，不能整批放开。

### 2. 两个待审池没有被系统转成可执行供给

- Dave 20条均为US/en，已有主页，系统给出的具体缺口是“缺近期内容标题或数据超过180天”。这类资料应先由系统自动刷新，不应把20条全部交运营逐个查。
- 食人花20条中19条路由KOL运营、1条例外路由Frankie；虽然都有主页，但没有主证据帖子。19条的系统说明把多种可能原因混成一段通用文案，只有1条写了具体审核原因，运营无法快速知道每条究竟缺什么。

最小修复：先自动补近期内容；仍不确定的记录必须逐条写明“只需判断哪一点＋证据链接＋通过/补资料/排除”，然后只把边界项派给运营。Frankie只处理真正的高预算或规则外例外。

### 3. 自动拓新来源已经耗尽

- Dave最近一轮需要5个新发现任务，但`keyword_source=none`，没有未使用的活动发现词；最近12个发现任务得到12个有效邮箱，但24小时内审核通过新增为0。
- 食人花最近一轮需要1个发现任务，DeepSeek返回`402 Payment Required`；受控降级词也没有继续产出未使用关键词，发现任务创建为0。
- 两项任务均没有有效在途发现任务，补池分项全部为0。

最小修复：

1. Dave优先把本活动已选择的NYXI未匹配证据作者接入自治补池，按20人小批持续补公开资料并通过硬筛；这只适用于本次选择了NYXI证据的Dave活动，不成为其他任务默认来源。
2. 食人花先消化现有20条待补资料，再补充不依赖外部模型付费状态的确定性七层词源（IP/平台/品类功能/用户场景/内容形式/市场语言），继续保留国家、语言、内容和重复触达硬门槛。

### 4. 15分钟业务审计能发现失败，但错误输出不可读

当前有效审计为`1WOenWodtTRlUqWz`，已启用并在02:12、02:27、02:42连续报错，说明它确实捕获了两项任务的`degraded`。旧小时审计`ijIcjoYO9Jm1Vdkw`保持停用是正确的。

但审计把时间解析成超大`age_seconds`，最终节点只显示类似`inventory_after_ma [line 13]`的残缺错误，运营无法直接知道“哪项活动、为什么卡、下一步做什么”。

最小修复：正确解析服务端时间格式，并输出固定业务摘要：活动、最新任务时间、业务结果、库存、额度、供给分项、明确卡点。审计报错本身不是当前停发根因，但会让无人值守系统变成必须靠Codex查日志。

## 会影响承诺与上稿、但不阻塞冷邮件的P1

1. 两项活动累计回复5条，但明确承诺和上稿均为0。现有回复监听在运行，说明当前主要问题是转化，不是漏抓到大量已承诺事实。
2. 两产品的活动价、KOL专属Deals、佣金兼容、有效期和最低毛利仍未定。它不阻塞首封冷邮件，但会在达人询价/谈条件时直接拖慢从“回复”到“承诺上稿”。
3. 人工上稿登记卡仍只写KOL主表，不能可靠写回活动参与记录；第一条人工上稿出现前必须补活动/产品选择和实际发布日期。
4. Follow-up、寄样对账、手动发送补登记仍按实例本地时间02:00、01:20、01:40执行，与名称中的10:00、09:20、09:40不一致。它们每天仍会跑，不是当前0库存的根因，但会影响运营时点和对账及时性。

## 不阻塞当前发信的P2/后续闭环

1. 活动级曝光、订单、GMV、费用和毛利归因尚未完成；影响后续预算倒推，不影响当前冷邮件生成。
2. 周一10:30上稿卡、11:00任务完成周报、11:30上稿×任务周报截至本次审计尚未到首次执行时点，需要当天回读真实execution；不能只看`active=true`。
3. 食人花18条已排除记录仍保留`参与状态=已入围`，发送闸会拦截，但人数口径和运营视图会被放大，应统一为已取消/已排除语义。
4. 人工直接改审核结论时，审核人/审核时间没有自动记录；本轮3条Dave记录和2条食人花记录存在审计留痕缺失。

## 建议最短执行顺序

1. 修复Dave“审核通过→全局路由仍待核对”的三条状态冲突，并单条回放确认只生成3条草稿、不触碰已有线程/近期触达对象。
2. 把Dave 20条待审核、食人花20条待补资料先做自动资料补全；只把仍有语义边界的记录形成可执行运营审核任务。
3. Dave接入本活动可选的NYXI证据作者小批续跑；食人花补确定性七层备用词，恢复真实供给。
4. 修复15分钟审计的时间解析和可读错误摘要；随后用定时任务回读证明库存和活动发信重新增长。
5. 再处理活动Deals与回复转承诺，以及人工上稿卡的活动归因。

完成标准不是“定时器显示成功”，而是连续两轮看到：新增合格候选或可审核候选、可发送库存大于0、发送中心实际消费库存、所有跳过项有稳定原因代码。
