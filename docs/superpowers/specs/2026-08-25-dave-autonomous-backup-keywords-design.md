# Dave 自动备用词续供设计

日期：2026-08-25  
状态：方案 A 已由 Frankie 确认，等待书面规格复核

## 目标

当 Dave 集中宣发活动的固定词、旧灰度词和已创建任务全部耗尽时，系统仍能按活动和产品资料自动生成未使用的 YouTube 创作者发现词。它替代的是运营逐轮人工补词这一重复决策，不改变 KOL 是否合格的判断门槛。

## 当前问题

生产自然轮次 `launchruntime-474ed084e29b` 已证明人工待审没有阻塞主流程，但 Dave 的确定性词源已经耗尽；当日 DeepSeek 预算又达到 60/60，因此发现任务为 0、可发库存仍为 0。当前自治入口只使用 Dave 旧固定词，已验证的结构化词仅在手动灰度入口启用，没有进入小时级自动补池。

## 方案

在 `ensure_campaign_supply()` 的 Dave 自动路径中，按以下顺序取词：

1. 继续优先使用尚未用过的旧固定词，保持兼容。
2. 固定词不足时，调用 Dave 七层确定性备用词编译器。
3. 备用词仍不足时才报告真实缺口；本次不提高 DeepSeek 预算，也不依赖模型才能续供。

七层信息为：

| 层级 | Dave 示例 | 作用 |
|---|---|---|
| 可选竞品 | 仅当活动明确选择且证据已就绪时使用活动配置中的品牌 | 不把 NYXI 写成全局默认 |
| IP/主题 | Dave the Diver、cozy indie game、fishing game、restaurant management game | 找到游戏/IP及邻近兴趣受众 |
| 主机平台 | Nintendo Switch 2、Nintendo Switch | 锁定目标主机生态 |
| 品类/功能 | controller、gamepad、gaming accessory | 锁定硬件评测创作者 |
| 玩家场景 | indie gamer、cozy gamer、Nintendo fan、handheld gamer | 扩充邻近受众 |
| 内容形式 | review、gameplay review、unboxing、hardware test、setup tour、accessories roundup | 把主题变成创作者搜索意图 |
| 市场本地化 | en、de、es | 按活动国家和语言生成当地表达 |

每个词至少组合两个信息轴；IP词优先组合平台或内容形式，避免再次出现“Dave人名、真实潜水、垃圾箱潜水”等歧义。词源按来源轮转，避免一个来源占满整批；英语为主，同时保留德语和西班牙语覆盖。

## 数据流

```text
活动/产品字段
  → 读取全部历史活动词并去重
  → 旧固定词中选未使用项
  → Dave七层编译器按来源和语言轮转补足
  → 购买/店铺/官方意图过滤
  → 每轮最多3～9个爬虫任务
  → 现有爬虫、画像、活动预检、草稿与唯一发送中心
```

任务名继续写入活动 ID，并为结构化备用词增加 `词源`，方便按来源回放有效邮箱、合格候选和排除原因。已有国家、语言、活跃度、内容、邮箱、历史关系、重复触达及发送证书闸全部保持不变。

## 低产与停止规则

- `volume_priority=true` 且可发库存不足时，可以覆盖发现冷却继续建立有上限的新词任务，但绝不降低候选筛选标准。
- 普通非体量优先运行仍遵守现有低产冷却和慢速探测。
- 历史任务中的完整关键词全局去重；同一词不得因语言或任务名不同而重复创建。
- 如果确定性备用词也全部用完，必须返回 `shortfall_tasks` 和明确原因，不能把“定时器成功”冒充供给成功。

## 测试边界

公开测试入口为 `ensure_campaign_supply()`：

1. 模拟 Dave 旧固定词和已生成结构化词全部耗尽，仍可得到4个未使用备用词。
2. 4个词至少覆盖3个来源，语言分布为英语主、德语和西班牙语辅。
3. 竞品模式关闭时，任何词不得出现 NYXI 或历史竞品名。
4. 购买、价格、优惠、店铺、官方频道意图继续被拒绝。
5. 真实写任务时保持目标国家/语言、每批50、任务状态待触发、活动ID和词源标签。
6. `quality_filters_lowered=false`，且备用词不消耗 DeepSeek 调用。

## 上线与验收

- 只修改关键词供给与相应测试，不改 n8n、邮箱额度、候选筛选、草稿模板或发送器。
- 部署前确认 Dave 和食人花没有正在运行的后台补池任务。
- 部署后不手动触发，等待 Dave 整点自然轮次。
- 完成标准：自然轮次创建未使用的 Dave 备用词任务，`keyword_source` 可追溯、`shortfall_tasks=0`、`quality_filters_lowered=false`；若爬虫后仍无合格候选，按真实低产结果继续换源，不把任务数量当成合格候选。

## 非目标

- 本次不把 Dave/NYXI 固化成所有产品的默认竞品。
- 本次不提高 DeepSeek 日预算。
- 本次不把待审核候选直接生成草稿或发信。
- 本次不把 Dave 专用灰度直接声明为所有产品的通用正式规格；待本轮真实产出验证后再抽象成通用产品词池。
