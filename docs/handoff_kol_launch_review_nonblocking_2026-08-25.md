# KOL集中宣发：待审旁路与体量优先修复交接

## 结论

本次把活动候选的人审从“主流程闸门”改为“并行旁路”。边界候选继续留给KOL运营批量审核，但不会阻止系统继续寻找、筛选、补入和发送其他明确合格对象。

## 问题与根因

1. `append_auto_approved` 默认在存在任何待审核对象时直接返回，通用活动入口会停止补入。
2. 即使去掉直接返回，旧容量公式仍把待审对象计入池目标；当20名待审占满20名目标时，系统仍会静默停止。
3. 食人花低产后会进入 `quality_cooldown`，整轮创建0个发现任务；在邮箱仍有额度、可发送库存仍不足时，这与集中宣发的时限目标冲突。
4. 已通过但草稿尚未生成的对象未计入在途供给；队列变慢时，连续轮次可能对同一缺口重复扩池。
5. 零模型预演未启用体量优先，可能显示0个发现任务，而真实运行创建3～9个，预演与真跑不一致。
6. 已通过但草稿生成永久失败的对象若无限期算作在途供给，会令系统假性占满并停止后续扩池。
7. 旧待审记录自动转通过时若不写新时间戳，会立即被30分钟TTL判过期；坏邮箱若先进入实时预检异常，也无法执行永久失败退出。

## 改动

- `app/launch_runtime.py`
  - 所有活动入口统一允许待审与明确合格对象并行。
  - 待审对象不占用明确可开发池目标名额。
  - 待审对象仍不得生成草稿或发送。
  - 有效供给按“可发草稿＋已通过待生成草稿”计算，避免连续轮次重复扩容。
  - 确定性永久失败（重复触达、状态冲突、坏邮箱）立即改为“已取消/不再符合”；临时失败只在30分钟内算作在途，超时自动释放容量。
  - 草稿队列优先处理最近入围对象，防止旧失败项长期占满处理批次。
  - 自动转通过时写入当前审核时间；邮箱清洗提前到实时预检前，保证TTL起点与永久退出分支真实有效。
  - 自治补池与日常反馈均按非待审活跃数计算追加上限。
- `app/keyword_supply.py`
  - 新增 `volume_priority` 控制；活动有库存缺口时，低产冷却改为继续有上限地换词发现。
  - 零模型预演与真实运行统一使用体量优先参数。
  - 保留每轮任务上限、在途去重、模型预算及全部硬筛选。
- `tests/test_launch_runtime.py` / `tests/test_keyword_supply.py`
  - 增加20名待审占满目标仍继续补入的边界测试。
  - 增加食人花低产冷却仍继续发现、但不降低筛选条件的测试。

## 验证

- 接入最新生产基线后聚焦测试：82项全部通过。
- 接入最新生产基线后全仓测试：763项＋23个子样例通过。
- 唯一失败：既有 `test_zeabur_watchdog...failed_deployment` 旧日期fixture；仓内历史交接已多次记录，与本次KOL文件无交集。
- `compileall`、`git diff --check` 通过。
- Spec与Standards最终复审均通过，无P0/P1/P2。

## 生产状态与下一步

- 已部署 commit `80d4f99`，Zeabur deployment `6a8c87cdba5938b757238ed7` 为 `RUNNING`，`/health=status ok`。
- n8n工作流 `uvBfJBtGH93FPa6w` 全程保持 `active=true`；没有手动触发补池、没有额外发送消息或邮件，也没有修改n8n和生产环境变量。
- 食人花首个有效自然轮次：execution `1006003` → job `launchruntime-2e60616da618` → `success/supply_in_progress`。18名待审继续留在旁路，不占有效库存；本轮清退10条永久失败旧对象并创建5个七层发现任务，`volume_priority=true`、`quality_cooldown_overridden=true`、`quality_filters_lowered=false`。证明待审不会阻塞继续发现。
- Dave首个有效自然轮次：execution `1006231` → job `launchruntime-474ed084e29b` → `degraded/supply_blocked`。16名待审被保留在旁路，系统仍继续刷新16份画像、扫描20名NYXI证据作者并清退6条永久失败旧对象，说明本次“待审不阻塞”修复生效；但没有新增合格候选、草稿或发现任务。
- Dave剩余阻塞与人审无关：现有七层发现词已经全部使用，确定性词库无法再生成4个新词；当日模型预算也已用满，无法补充新词，因此可发库存仍为0。下一步应单独补充Dave备用词供给，不能把这次零产出归因于等待审核。

## 2026-08-25 方案 A2 开发补充：Dave 外部 YouTube 发现词续供

### 口径纠正

此前“Dave备用词用完”的说法不准确。旧逻辑只是把爬虫任务台里出现过的关键词字符串永久去重，并不能说明：

1. KOL主表没有合适对象；
2. YouTube搜索不到作者；
3. 某个词源已经真实连续低产。

方案 A2 将结果拆成三层：`internal_pool`（主表筛选人数）、`external_youtube_discovery`（站外YouTube任务数）、`external_youtube_outcome`（站外任务完成并回流后的结果人数）。当前发现词只用于部署终端在YouTube站外搜索，不是拿这些词在KOL主表做全文匹配，也不同时搜索TikTok/Instagram。

### 本轮开发改动

- 历史去重键改为“Unicode NFKC规范化关键词＋排序国家＋语言”。在途任务始终去重；只有`3-已完成`进入7天窗口；失败、取消及缺国家/语言的旧记录不再错误阻断其他市场。
- 超过7天允许重跑，顺序为：历史有可用对象的组合优先、未知其次、明确0产出最后；新组合仍优先于历史重跑。
- 正常活动预览只接受部署终端写入的“当前活动ID＋爬虫任务record_id＋词源＋任务关键词”完整标记，按具体任务轮次回溯明确通过、运营审核和硬筛排除人数；孤立词源、同关键词旧记录及其他活动记录统一归为`unknown`。
- 每个已完成任务保留原始发现、有效邮箱、主表写入量和主要排除原因；KOL进入草稿或已有线程后，来源成效沿用活动参与记录中首次筛选分流，不会在下一轮被重算成0。
- 来源连续两轮可用产出为0时冷却2小时；冷却后只允许1条恢复探测。已有同来源在途探测时，下一自然轮次不会重复追加。
- 探测只有回流后至少产生1名“系统建议通过或可运营审核”对象才恢复正常轮转；有邮箱但内部结果未知时仍保持受控探测。
- 已完成任务缺词源、执行日志或完成时间时返回`source_signal_unavailable`并限制为全活动1条探测。A2以前受控的Dave固定词可按固定词表确定性还原为`legacy_fixed`；其他无标签任务不会被笼统伪装成该来源。
- `raw_discovered`只读明确的原始发现信号；“新增＋更新”单列为`records_written`，不再冒充原始发现数。
- 自治补池和Dave灰度入口共用活动级写锁，避免两入口同时读取旧快照后创建重复任务。

### 验证与状态

- 云端专项回归：159项通过；部署终端追溯标记专项：10项通过。
- 全仓回归：583项中，本次相关测试全部通过；14个导入错误来自本地测试环境缺`fastapi/pytest/cryptography/tzdata`，另有既有Zeabur watchdog旧日期fixture失败，均与本次KOL改动无交集。
- `compileall`和`git diff --check`通过。
- 当前仅完成开发和本地验证，尚未部署；生产仍为commit `80d4f99`。n8n工作流未启停、未手动触发，未发送邮件或飞书消息。

### 剩余闸门

1. 等待规格与工程双轴最终复审通过后提交云端与部署终端两个代码仓库。
2. 生产部署需要Frankie另行明确授权；部署前确认Dave、食人花后台补池job和爬虫任务表都没有运行中任务。
3. 部署终端必须按单实例闸发布：停止旧daemon → 更新包含`discovery_trace.py`在内的完整commit → 只启动1个daemon → 验证普通任务扫描正常；不得让新旧daemon重叠抢同一任务。
4. 部署终端验证后才发布云端A2；云端仍保持单副本，n8n工作流不启停、不手动触发。
5. 部署后只等自然cron，先验收Dave创建的YouTube任务，再在任务完成后的下一轮验收来源归因和冷却/恢复，不以HTTP或n8n success代替业务结果。

## 2026-08-25 生产发布与自然轮次验收

### 发布结果

- 部署终端仓库已更新到 `e54cfe0`。计划任务 `KOL-Scraper-Daemon` 正常，Windows显示的两个Python进程是“启动器父进程＋实际解释器子进程”，启动时间一致，属于1个逻辑daemon；新启动日志存在且无`Traceback`。
- 云端分支在最新 `origin/master` 上重新整合并跑完目标测试：`160 passed, 6 subtests passed`，`git diff --check`通过。
- 云端生产commit为 `589130993632dea40707ab0590f1640cd32fca19`，Zeabur deployment `6a8d82f03395ae1227601cdd` 为 `RUNNING`，`/health`返回`status=ok`。
- n8n工作流 `uvBfJBtGH93FPa6w` 始终为 `active=true`，活动版本与当前版本一致。发布期间没有手动触发、没有启停或改写n8n、没有修改环境变量，也没有额外发送消息或邮件。

### Dave A2 自然轮次

1. 20:00自然execution `1012745` → job `launchruntime-b469a9ab45eb` → `success/supply_in_progress`：创建1条站外YouTube任务，`quality_filters_lowered=false`。
2. 部署终端自然完成首条 `legacy_fixed` 任务：搜索50个频道、写回1条新主表记录，但有效邮箱0；无报错。
3. 21:00自然execution `1013101` → job `launchruntime-b399940d29fc` → `success/supply_in_progress`：正确回读首条任务的`raw_discovered=50`，随后自动创建第二条西语 `legacy_fixed` 任务。
4. 第二条任务自然完成：搜索50个频道、有效邮箱0、主表新增0；无报错。
5. 22:00自然execution `1013474` → job `launchruntime-fb15568c88b2` → `success/supply_in_progress`：系统确认 `legacy_fixed` 连续两轮零可用候选，将该来源切为 `cooldown`，并自动换到 `competitor` 来源创建 `nyxi controller test deutsch`。
6. 换源任务已被部署终端自然完成：搜索12个频道、有效邮箱0、主表新增0；无报错。

### 验收结论

- A2已证明完整自动闭环：云端按库存缺口创建站外任务 → 部署终端自动搜索并写回 → 下一自然轮次按任务记录回读 → 连续低产自动冷却 → 自动换源继续，不依赖Codex会话或人工逐轮触发。
- 三层数字口径生效：内部主表筛选、外部任务创建、外部任务结果分开统计；`raw_discovered`没有再用“新增＋更新”冒充。
- 本次真实搜索没有形成可发送候选，原因是新任务均未取得有效邮箱，不是人工待审、服务异常或筛选条件过严。全程`quality_filters_lowered=false`，没有为凑数量放宽国家、语言、邮箱、历史关系或重复触达门槛。
- 食人花发布后自然轮次也能正常运行，但当前仍为`supply_blocked`：最近验收轮次仅完成资料刷新，没有新增候选、草稿或发现任务；这是独立的供给问题，不影响Dave A2验收结论。

### 当前剩余事项

1. `P0`：单独修复食人花持续`supply_blocked`，重点检查为什么体量优先下仍没有建立新发现任务。
2. `P1`：继续通过自然轮次观察Dave其他来源的有效邮箱与可用候选产出；A2系统机制已验收，不需要再由Codex手动触发。
3. `P2`：Dave灰度获得稳定正样本后，再抽象为其他产品可选的通用词池规格；竞品仍必须由活动配置选择，不把NYXI固化为默认来源。
