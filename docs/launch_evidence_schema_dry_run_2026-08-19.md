# KOL 活动竞品证据与参与记录：生产表结构只读预检

状态：G1 已完成；等 Frankie 明确批准后才可进入 G2 写入。

## 结论

本次只读取生产飞书 Base 的表和字段，没有创建表、字段或记录，也没有部署、开功能开关、发卡或发邮件。

生产表结构可以按“只新增、不删除、不改名”方式继续实施，但 G1 发现并先修正了两处本地定义偏差：

1. 节点表原迁移清单漏了 4 个运行时会写入的字段；若不补，开启新调查后会在生产报字段不存在。
2. KOL 主表与竞品帖子表的实际稳定身份字段分别是 `YouTube频道ID / 主链接` 和 `KOL平台ID / KOL主页URL / KOL账号Handle`；本地匹配器已补齐这些真实字段，避免只依赖“关联KOL”关系才能匹配。

修正后专项测试 60/60 通过。生产 Base 未被改变。

## 只读对象

| 对象 | 表 ID | 当前记录数 | 当前 rev | G1 结论 |
|---|---|---:|---:|---|
| 新品集中上稿活动（影子） | `tbl8w0O7pI5PsRnq` | 2 | 12 | 表存在；需新增 19 个字段 |
| 新品集中上稿节点（影子） | `tblUljeSSAvFdFT6` | 16 | 1 | 表存在；需新增 9 个字段 |
| 新品集中上稿活动参与记录 | 尚无 | 0 | — | G2 需新建 1 张表及 26 个字段 |
| 竞品帖子监控库 | `tblCDbvLtnLzdxEp` | 3,423 | 4,786 | 证据来源表存在，关键字段满足校验 |
| 竞品新品与营销事件 | `tblpZaWYEWy54Sll` | 7 | 23 | 事件来源表存在，关键字段满足校验 |
| 海外KOL主表 | `tblMMhnj2hEbhF6y` | 7,674 | 18,042 | 现有身份字段可用于稳定匹配 |

Base：`KINabIENjak8fRsB6AHcIDALntc`。读取身份：当前飞书 user 授权。全库仍为 27 张表，没有名为“新品集中上稿活动参与记录”的表。

## G2 精确差异

### 活动表：新增 19 个字段

现有 `活动ID` 等影子规划字段继续保留；以下字段均不存在，且没有同名异类型冲突：

- 证据配置：`产品主记录ID`、`竞品证据模式`、`竞品分析状态`、`竞品品牌`、`关联竞品营销事件`、`关联竞品帖子`、`证据快照时间`、`证据排序版本`、`证据配置版本`
- 名单授权与 KOL 状态：`名单锁定授权`、`KOL已锁定名单版本`、`KOL名单阻塞代码`、`KOL失败锁定批次ID`、`KOL阻塞待处理记录`
- 媒体人状态：`媒体人已锁定名单版本`、`媒体人名单阻塞代码`、`媒体人失败锁定批次ID`、`媒体人阻塞待处理记录`
- 说明：`证据等待/变更说明`

差异：新增 19、复用 0、冲突 0、删除 0、改名 0。

### 节点表：新增 9 个字段

旧字段 `状态 / 阻塞说明` 不删除、不改名；活动证据运行时使用以下独立字段，避免改变现有影子节点含义：

- `节点状态`、`节点阻塞说明`、`竞品品牌`、`目标证据配置版本`
- `待确认竞品帖子`、`待确认竞品事件`
- `调查提交版本`、`调查提交时间`、`调查提交说明`

差异：新增 9、复用 0、冲突 0、删除 0、改名 0。

### 活动参与记录：新建 1 表、26 个字段

- 身份与关联：`参与记录ID`（主字段）、`活动ID`、`关联活动`、`对象类型`、`关联KOL`、`关联媒体人`、`产品家族ID`
- 排序与证据：`进入方式`、`基础评分快照`、`竞品证据等级`、`关联竞品帖子`、`最终优先级`、`选择原因`、`排序版本`、`排序快照历史`
- 状态与版本：`参与状态`、`锁定批次ID`、`名单版本`、`取消原因代码`
- 执行关联：`关联KOL任务`、`关联媒体人任务`、`关联邮件草稿`
- 上稿结果：`计划上稿时间`、`承诺上稿时间`、`实际上稿时间`、`上稿链接`

差异：新建 1 表、26 个字段；因为表尚不存在，所以没有存量字段冲突。

## 现有证据字段兼容性

| 来源 | 已确认字段 | 用途 |
|---|---|---|
| 竞品帖子监控库 | `竞品品牌`、`人工复核状态`、`相关性`、`合作信号`、`关联KOL`、`曝光量`、`覆盖量`、`发布时间`、`平台`、`内容类型` | 证据合格校验、效果和长期合作判断 |
| 竞品帖子监控库 | `KOL平台ID`、`KOL主页URL`、`KOL账号Handle`、`KOL账号名` | 无直接关联时的稳定身份匹配 |
| 竞品新品与营销事件 | `竞品品牌`、`人工确认状态`、`关联帖子` | 历史事件引用和人工确认 |
| 海外KOL主表 | `竞品帖子证据`、`主平台`、`账号名`、`YouTube频道ID`、`主链接` | 候选人与竞品帖子去重匹配 |

## G2 写入边界

获批后，G2 只能做以下动作：

1. 在活动表新增上述 19 个字段。
2. 在节点表新增上述 9 个字段。
3. 新建活动参与记录表及 26 个字段。
4. 部署本地已测试版本，但只开启 `LAUNCH_EVIDENCE_ENABLED`；`LAUNCH_PARTICIPATION_WRITE_ENABLED` 继续保持 `0`。
5. 只为 Dave 活动配置 NYXI 证据并生成只读候选预览；不锁名单、不建任务、不建草稿、不发邮件。

以下动作不在 G2：删除/重命名现有字段、修改旧节点记录、写活动参与名单、真实触达、寄样、付费、运营群发卡。

## 验证标准

G2 完成后必须回读：

- 19 + 9 + 26 个字段的名称、类型和关联目标完全一致；没有重复同名字段。
- 两张现有影子表记录数仍为 2 和 16，旧字段值不变。
- Dave 证据预览 `writes=0`，食人花及日常派单排序不受 NYXI 证据影响。
- 参与记录表仍为 0 行，参与记录写开关仍关闭。
