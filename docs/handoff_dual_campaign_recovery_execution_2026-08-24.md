# Dave＋食人花最短恢复执行记录（2026-08-24）

## 结论

本轮把会直接卡住两项活动的四个断点收进原有活动控制器：人工审核后的路由提交、待审资料自动刷新、活动来源续供、15分钟审计可读性。真实邮件仍只经过原统一发送中心；没有新增第二发送器。

## 问题与影响

| 优先级 | 问题 | 业务影响 | 本轮动作 |
|---|---|---|---|
| P0 | Dave三名人工通过者仍停在全局路由待核对 | 审核通过也无法进入草稿队列 | 只在唯一阻塞为待核对且无历史冲突时提交路由；已有线程或近期触达继续拦截 |
| P0 | 两活动待审池只刷新主表，不重算活动参与记录 | 运营反复看到缺资料，系统无法自动放行确定项 | 刷新后重算参与记录；确定项自动通过，边界项保留具体人工指令 |
| P0 | Dave证据与食人花词源无法持续续供 | 可发库存为0，邮箱额度闲置 | Dave只续取本活动已选择的竞品证据；食人花启用可追溯七层确定性词源 |
| P0 | 审计时间解析错误且摘要截断 | n8n虽然报错，运营不知道哪个活动、为什么卡、下一步做什么 | 兼容`+0000`时间；错误摘要固定给出活动、库存、供给状态和下一步 |
| P0 | 部署中断后的后台任务残留running | 看起来“还在跑”，实际已经停止 | 持久任务无当前进程时改为明确中断错误，允许安全重跑 |

## 改动范围

- `app/launch_runtime.py`：待审资料重算、Dave证据自治续供、证据窗口游标、多个后台job独立留存、结果摘要保留、重启状态收口。
- `app/launch_evidence_author_import.py`：Dave活动证据续供，每轮最多3名待审核对象。
- `app/launch_competitor_evidence.py`、`app/launch_candidate_preview.py`：证据作者稳定续取窗口。
- `app/keyword_supply.py`：食人花七层确定性词源，竞品层仅按活动显式配置开启。
- `app/main.py`：后台任务入口、写入确认闸和重启恢复。
- `scripts/upsert_launch_autonomy_workflows.ps1`：15分钟业务审计按结果/额度/库存/供给分项判定；错误含最新时间与下一步；全量PUT保留现有节点扩展属性和未受管分支。

## 安全边界

- Dave续供只允许`launch-20260915-funlab-dave-ys11-5`，且必须从服务端当前活动证据重新核验。
- 每轮最多导入3名，初始状态为待审核；不创建草稿、不发送邮件。
- 食人花竞品层默认关闭；只有活动选择“新调查/引用历史”且证据已就绪才开启，不写死NYXI。
- 全局重复触达、同产品历史、活动锁、滚动24小时额度和邮件raw校验保持不变。

## 验证

- 修复后相关回归：91项＋3个子样例通过。
- 全仓：604项＋23个子样例通过；唯一失败为既有Zeabur watchdog旧日期fixture，与KOL改动无关。
- 最终生产版本：commit `18645f81ff970f57e101f98cb26bdc4f1d30525b`，Zeabur deployment `6a8b5b37ba5938b7572356ef`，状态RUNNING，健康检查通过。
- Dave任务`launchruntime-06db888831f0`完成：待审资料刷新20、参与记录更新19；证据窗口17→37，20名合格0、业务写入0；业务结论为`supply_blocked`，系统明确标红而非冒充成功。
- Dave 3份已审核草稿已由既有统一发送中心真实发出：飞书发送状态/时间/发件品牌回读正常，正文长度815–830、无发送错误；Zoho发件箱逐条唯一命中、主题一致、message_id存在；活动Raw证书9项检查全通过。
- 食人花两次自治回放均为`supply_cooling_down`：当前0库存、120余量；质量闸因最近审核转化为0冷却至05:39:35，冷却结束后再验证七层词源真实创建。
- 15分钟审计已完成真实验收：04:42 execution `997485`只显示异常Dave，错误标题完整给出最新时间、库存0、余量114、四项供给0和下一步；健康食人花不再导致摘要截断。
- 04:35食人花轮次正常完成为`supply_cooling_down`，没有误报、没有越过质量闸。生产最终验收尚需：05:00后验证Dave证据游标从37继续；05:50后回读食人花七层词源首轮真实产出。

## 剩余风险与回滚

- 全量候选预览耗时较长，最终部署期间不得再次推送同服务仓库，避免中断后台任务。
- 若Dave续供没有新合格作者，结果应明确为0，不得以扫描量冒充进展；继续移动证据窗口或转回关键词/历史关系来源。
- 食人花质量闸不可为了“用满额度”而跳过；只有冷却到期后才能创建新发现任务。若七层词源仍连续0审核通过，应调整来源层权重或补资料采集能力，而不是降低国家/语言/内容/邮箱硬门槛。
- 回滚以提交为单位；n8n修改必须先GET完整工作流，保留未管理节点、连接、设置和active状态后再PUT。
