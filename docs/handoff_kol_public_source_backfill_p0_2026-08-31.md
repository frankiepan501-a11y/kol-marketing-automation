# KOL 主表公开落地页与邮箱补全 P0 交接

日期：2026-08-31

## 结论

本次 P0 已把“从公开主页保存官网/Linktree/Beacons 等落地页，再从公开落地页补严格验证邮箱”固化进云端补数工具和本地 KOL 采集器。生产只做了 30 条跨平台受控试跑，没有全表盲写，也没有发送邮件或飞书卡片。

## 业务结果

| 项目 | 结果 |
|---|---:|
| KOL 主表总量 | 9,528 |
| 受控试跑 | 30 |
| 公开来源字段写入并回读成功 | 12 |
| 严格验证邮箱写入并回读成功 | 3 |
| 重复邮箱归属拦截 | 5 |
| 验证结果未知拦截 | 1 |
| 验证非有效拦截 | 1 |
| 覆盖已有值 | 0 |
| 并发变更或回读不一致 | 0 |

## 原问题与根因

- 旧采集器只保存 YouTube、TikTok、Instagram 等社交平台类别，没有保留频道公开外链里的官网或聚合页 URL。
- 主表虽然已有 `聚合页URL` 和 `其他链接` 字段，但缺少统一的“公开证据 → 邮箱验证 → 查重 → 写回”规则。
- 公开页面出现邮箱不等于邮箱可用；如果直接写回，会把错误或他人邮箱带入开发信流程。

## 本次规则

1. Linktree、Beacons 等聚合页写入 `聚合页URL`；官网和其他公开非社交落地页写入 `其他链接`。
2. 只接受平台公开资料里的明确链接，不猜官网，不把搜索摘要当证据。
3. 任何已有字段都不覆盖；写前重读，写后逐条回读。
4. 公开页面邮箱必须与当前 Snov 查询返回的有效邮箱完全一致。
5. 同一邮箱已属于另一 KOL 时拒绝写入。
6. 批次检查失败时零写入；发生并发变化、写入错误或回读不一致时立即停止后续写入。

## 代码版本

- 云端 `kol-marketing-automation`：`f9ef293`（基于 `origin/master=23e8c23` 安全整合，交接文档提交后以最终发布 commit 为准）。
- 本地部署终端 `kol-scraper-daemon`：`0a03fc4`。
- 本地采集器只保存公开来源，不直接写“有效邮箱”；严格邮箱判断统一留在云端。

## 验证

- 云端专项：33 项通过，5 个子测试通过。
- 云端完整回归：914 项通过，31 个子测试通过。
- 唯一失败是既有 `test_zeabur_watchdog` 固定日期测试：模拟失败部署时间为 2026-07-07，当前运行时已超出测试设置的 24 小时回看窗口，与本次 KOL 改动无关。
- 本地 daemon：30 项测试通过。
- 两轮代码审查均无阻断问题。

## 生产安全边界

- 未发送任何开发信或运营卡片。
- 未改 n8n 工作流、环境变量或 KOL 质量筛选标准。
- 生产主表不保存猜测邮箱。
- 飞书读取出现 `1254607 Data not ready` 后，公开来源读取并发已由 5 降为 2；检查失败会停止，不会带病继续写。

## 剩余风险与下一步

- P0 发布后需由陈翔宇部署终端执行一次 `git pull --ff-only` 和单实例重启；合格标准是 1 个逻辑 daemon（2 个父子 Python 进程）、版本正确、启动后无 Traceback。
- 单词型频道名、缺少可拆分姓名或 Snov 无结果的对象会保持邮箱为空。这是“宁缺勿错”的预期结果，不应降低质量门槛。
- 本轮只验证 30 条受控样本。扩大到更多记录前，应继续按小批次、低并发、100% 回读方式执行。
- P1 可补充单条可回放报告，让运营看到“公开来源、验证结果、阻断原因”，但不要把整批资料搜集重新转给运营。

## 2026-09-01 邮箱读取稳定性加固

- 原问题：邮箱阶段对目标记录逐条调用飞书单记录接口；50 条只读演练会偶发 1–2 条 `1254607 Data not ready`，导致整批安全中止。
- 修复：一次通过稳定列表接口读取 `账号名/邮箱/邮箱验真状态/主链接/聚合页URL/其他链接`，建立本地索引；重复邮箱所有者检查复用同一快照。
- 没有削弱生产保护：正式写入前仍重读最新值，写入后仍回读核对；本次没有正式写入。
- 分级验收：10/10、30/30、50/50 均完成，三档 `writes=0`、`safe_to_continue=true`、`processing_error=0`。
- 50 条业务结果：40 条没有公开邮箱，10 条验真服务暂不可用；公开来源侧发现 7 条链接候选但未写入。
- 回归：相关 34 项 + 5 个子测试通过；完整 915 项 + 31 个子测试通过。唯一失败仍是既有固定日期 Zeabur 看门狗用例，与本次改动无关。
- 证据文件：`C:/tmp/kol-empty-email-public-source-bulk-10-20260901-evidence.json`、`C:/tmp/kol-empty-email-public-source-bulk-30-20260901-evidence.json`、`C:/tmp/kol-empty-email-public-source-bulk-50-20260901-evidence.json`。
- 当前边界：本次改动尚未部署，也没有授权正式批量回填邮箱或公开来源字段。

## 2026-09-01 验真状态纠偏与下一批 50 条

- 纠偏：上一轮“10 条验真服务暂不可用”全部是单词型账号名无法拆成姓+名，不是 Snov 授权或接口整体故障。
- 报告修复：现已分别显示 `no_public_email`、`verification_input_insufficient`、OAuth、Finder HTTP、网络和超时；只改变可观测性，不改变邮箱写入条件。
- 下一批 50 条生产只读结果：42 条没有公开邮箱、4 条姓名输入不足、2 条 Finder HTTP 400、2 条 Finder 超时。
- 安全结果：邮箱写入 0、公开来源写入 0、邮件 0、飞书卡片 0；`safe_to_continue=true`。
- 能力边界：当前 OAuth 正常、Finder 非整体失权；少量请求被上游拒绝或未在 28 秒内完成。它们继续保持邮箱为空，可在后续单条重试，不需要全表重跑，也不能降低 `valid` 标准。
- 证据：`C:/tmp/kol-email-verification-diagnostic-original50-20260901-evidence.json`、`C:/tmp/kol-empty-email-public-source-next50-20260901-replay-evidence.json`。证据只含汇总及 `record_id + 状态`，不含完整邮箱或凭据。
- 发布边界：本节的失败原因分类与脱敏证据改动当前仅在本地候选分支验证，尚未推送或部署；生产主表、环境变量、凭据和 n8n 均未修改。

## 2026-09-01 陈翔宇部署终端验收

- 本地采集器版本：`0a03fc4`。
- 重启后进程：PID 15864 → 14868，2 个父子 Python OS 进程 = 1 个逻辑 daemon。
- 两个进程启动时间：2026-09-01 10:06:07，同一次计划任务启动。
- 日志判断：截图中的 HTTP 400/Traceback 发生于 09:00–09:01，属于本次重启前的历史任务；10:06 新启动后未见 Traceback。
- 结论：部署终端验收通过，当前不需要陈翔宇继续操作。后续云端失败原因分类提交 `67d5579` 的推送/部署是独立发布步骤，不由陈翔宇终端执行。

## 2026-09-01 云端发布与部署后验收

- 最终云端发布 commit：`b7e283ebbbf9b580c375cf4b4aee4fd9f4ada5d4`（含核心分类修复 `67d5579`）。
- 发布方式：只做 fast-forward 推送；未改环境变量、凭据或 n8n，未手动重启或重部署。
- Zeabur deployment：`6a963b7b9ed7d65609e28e46`，状态 `RUNNING`，精确 commit 与目标一致。
- 健康检查：`/health` HTTP 200、`status=ok`、`kol_ai_configured=true`。
- 部署后 50 条只读对照：`no_public_email=42`、`verification_input_insufficient=4`、`verification_provider_http_400=2`、`verification_provider_timeout=2`；链接写入 0、邮箱写入 0。
- 对照结论：部署前后分类完全一致；系统没有把第三方验证失败误写成“无邮箱”，也没有因本次核验产生业务写入、邮件或飞书卡片。
- 证据文件：`C:/tmp/kol-empty-email-public-source-postdeploy50-b7e283e-20260901-evidence.json`。
- 当前 P0 已收口。后续 P1 应提升公开来源覆盖率和单条回放能力，不能通过降低邮箱 `valid` 门槛换数量。

## 2026-09-01 P1 公开联系页覆盖与单条回放

- 问题：已有抓取器能读取官网首页和部分 contact/about 页面，但有限页面预算没有优先给“联系我 / 合作 / work with me”等明确联系入口；遇到没产出时也无法解释哪一页抓过、哪一页被跳过。
- 修复：识别链接可见文字中的联系意图，兼容同站 `www`/裸域名，把聚合页外链按“明确联系页 → 官网 → 通用 shop/support”排序；总页面预算仍硬限制为 4 页。
- 可观测：新增单条 `record_id` 只读回放，逐页输出脱敏状态。证据只保留来源类型、域名、URL 指纹、抓取/跳过原因和候选数量，不保存邮箱明文、完整 URL、页面正文或上游原始报错。
- 生产回放修复：首次回放因飞书单记录接口连续返回 `1254607` 失败；已改用列表接口读取最小字段后按 `record_id` 本地定位。同一条复测成功，`write_attempted=false`。
- 质量闸门未变：只有公开原文明确出现邮箱、Finder 精确返回 `valid` 且返回同一邮箱、主表不存在重复归属时才具备写入资格；本轮没有生产写入。
- 验证：相关回归 53 项及 5 个子测试通过；完整回归 934 项及 31 个子测试通过。唯一失败仍是既有固定日期 Zeabur 看门狗用例。
- 同批 50 条 A/B：公开来源候选 12 条，与 P0 基线一致；邮箱状态仍为 42 条无公开邮箱、4 条输入不足、2 条 Finder HTTP 400、2 条 Finder 超时。链接写入 0、邮箱写入 0、邮件 0、飞书卡片 0。
- 证据：`C:/tmp/kol-empty-email-public-source-p1-ab50-894c87e-20260901-evidence.json`、`C:/tmp/kol-public-email-p1-replay-recvhwpbzeaYRv-20260901.json`。证据均不含邮箱明文或凭据。
- 发布边界：P1 当前只在本地候选分支完成；未推送、未部署、未改环境变量或 n8n，也不需要陈翔宇终端操作。若要上线，必须另行授权 fast-forward 发布和部署后只读复核。

## 2026-09-01 P1 生产发布与只读验收

- 用户已单独授权 P1 fast-forward 推送和生产部署。
- 发布前等待食人花自然轮次、本地爬虫和 n8n execution 全部排空；期间没有停止任务、强制结束进程或手动触发业务流程。
- 最终生产 commit：`64ab2dcddfe95d0f3b5eae41793c69bba93b6a7e`（含核心实现 `894c87e`）。从 `b7e283e` 仅做 fast-forward 推送，没有强推。
- Zeabur deployment：`6a9657e62aa889148228e4d9`，状态 `RUNNING`，commit 与目标精确一致。
- 健康检查：`https://kol-auto.zeabur.app/health` 返回 HTTP 200、`status=ok`、`kol_ai_configured=true`。
- 部署后复用部署前同一批 50 条生产只读回归。公开来源：12 条具备候选、38 条无新来源；邮箱：42 条无公开邮箱、4 条姓名输入不足、2 条 Finder HTTP 400、2 条 Finder 超时。
- 部署前后逐条状态完全一致；链接写入 0、邮箱写入 0、邮件 0、飞书卡片 0，`safe_to_continue=true`、`quality_filters_lowered=false`。
- 证据：`C:/tmp/kol-empty-email-public-source-p1-postdeploy50-64ab2dc-20260901-evidence.json`。
- 业务判断：P1 已补齐“单条为什么没找到/为什么没写”的追踪与回放能力，但这批 50 条没有新增邮箱产出，不能把可观测性上线误报为邮箱供给量已经提升。后续应换更有代表性的公开落地页样本评估真实增量，不能降低 `valid` 标准。
- 陈翔宇终端不需要本次云端 P1 操作；本地 daemon 继续保持已验收版本和单实例运行。
