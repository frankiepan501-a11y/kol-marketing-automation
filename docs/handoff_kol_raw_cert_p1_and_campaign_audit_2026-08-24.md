# KOL Raw证书P1与双活动审计交接（2026-08-24）

## 结论

- 多模板Raw证书P1已上线：不同邮件模板各自保存验证证书，不再互相覆盖。
- Zoho邮件服务与n8n内部授权已恢复并完成真实定时回归；当前不是技术发送故障。
- Dave与食人花的小时级自治补池已经恢复真实生产运行；当前共同技术阻塞已解除。两活动可发库存仍为0，下一瓶颈是待审候选转成合格草稿，不是邮箱额度不足。

## 本次改动

- `app/launch_raw_certificate.py`：新增schema v2、旧数据迁移、按模板合并保存和活动级写锁。
- `app/auto_send.py`：只读取活动生产模板`launch-queue-v1`对应证书。
- `tests/test_launch_raw_certificate.py`：覆盖迁移、分槽、并发合并、错误模板隔离。
- `scripts/recover_kol_zeabur_env_20260824.ps1`：仅通过增量接口恢复服务变量，不输出敏感值。
- `scripts/rotate_kol_internal_token_20260824.ps1`：完整GET工作流后只替换目标请求头，再完整PUT；保留工作流active状态。

代码提交：`3b2f8e80f568f0b13201cbd10a3ddd9226bbc1b2`。

## 生产验证

- `launch-queue-v1`与`kol-cold-template-v1`均在测试邮箱完成9项raw校验；生产草稿写入0。
- 活动记录`recvsFoRmeGj4Y`回读两个模板槽同时存在且均通过。
- 55个n8n调用节点的新授权全部一致；服务健康、非dry-run、未暂停。
- 2026-08-24 20:00自动发送execution `1003477`成功，Webhook调用无401。
- 聚焦测试111项；合并最新master后全仓676项通过。1项旧日期watchdog fixture仍失败，与本次改动无关。

## 事故与根因

执行P1期间误把Zeabur全量环境变量接口当成单变量接口，导致服务变量短时被覆盖。服务按设计失败闭锁，因此故障期没有真实KOL邮件发出。变量已完整恢复；因旧内部令牌曾出现在内部诊断输出中，已立即轮换并同步所有n8n调用节点。

以后修改单个Zeabur变量，禁止使用`updateEnvironmentVariable`；必须使用单变量增删改接口，并在部署前后核对变量总数、关键项和真实业务定时执行。

## 当前活动事实

| 活动 | 今日/累计发信 | 今日/累计回复 | 承诺 | 上稿/目标 | 可发库存 | 品牌邮箱滚动24小时 |
|---|---:|---:|---:|---:|---:|---:|
| FUNLAB Dave | 24 / 176 | 5 / 9 | 0 | 0 / 20 | 0 | 34 / 120 |
| POWKONG 食人花二代 | 0 / 18 | 0 / 1 | 0 | 0 / 20 | 0 | 1 / 120 |

## 剩余卡点

1. **P0：待审候选尚未转成可发库存。** 21:00 Dave真实轮次新建14条待运营审核候选；21:05食人花真实轮次新建15条待审候选。两边自动通过、草稿和可发库存仍为0，现有审核/爬虫链需要继续把候选转成合格草稿。
2. **P0：食人花供给质量仍受冷却保护。** 21:05已创建1个七层确定性发现任务；21:20再次运行时因近期有效邮箱率和自动通过转化不足进入质量冷却，没有降低国家、语言、内容、邮箱或重复触达硬闸。
3. **P1：承诺与上稿结果闭环。** 两活动承诺和实际上稿均为0；需完成回复→承诺→实际上稿的活动唯一归因，不能只靠发信量判断完成。
4. **P1：Deals与最低毛利边界。** 未确认前报价与付费合作继续人审，但不阻塞冷开发首触。

## 建议最小下一步

自治工作流`uvBfJBtGH93FPa6w`已经重新启用并完成真实自然任务验收。下一步不再手动触发补池：由系统按既定频率运行，运营只处理已经带判断证据的待审候选；日报继续同时监控“自治是否健康、待审量、可发库存、品牌额度、承诺和上稿”。

## P0自动补池恢复证据

- 工作流：`active=true`，时区`Asia/Shanghai`；Dave每小时00分，食人花每小时05/20/35/50分。
- 生产模式：两个受管节点均显式通过生产双闸；默认只读模式未被全局放宽。
- Dave：execution `1003858` → job `launchruntime-de8ab949c278` → `success/supply_in_progress`；14条待审候选、1张运营审核通知、草稿0、邮件0。
- 食人花：execution `1003904` → job `launchruntime-d7bb14efbd30` → `success/supply_in_progress`；资料写入9、发现任务1、待审候选15、草稿0、邮件0。
- 后续自然轮次：execution `1003987` → job `launchruntime-add7c3f17ee5` → `success/supply_cooling_down`；资料写入15，保留现有15条待审，没有重复建候选或发邮件。
- 持续触发验证：重新激活后的21:35 execution `1004081` → job `launchruntime-d1340d282a17`同样成功；资料写入15、待审仍为15、草稿0、邮件0，证明定时器不是只生效一次。
- 独立审计：execution `1004032`为success；Dave业务结果正常，食人花当时在正常后台窗口内，随后任务已回读成功。
