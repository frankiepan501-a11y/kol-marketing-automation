# KOL 集中宣发 P0 修复交接（2026-08-31）

## 业务结论

本次只修复三项已授权问题，不重做 KOL 集中宣发系统：

1. Dave 7 条已有邮件线程的对象不再进入“新开发待审”；它们退出运营队列，继续沿原线程处理。
2. 食人花 `HandheldBOSS` 已是“不合适”对象，不再进入运营待审；系统直接排除。
3. 对食人花资料过期和 Dave/食人花邮箱问题做限定批次修复；只有可核验为 `valid` 的邮箱才允许写入，无法证实的邮箱保持原状。

这次改造解决的是 L3 交接缺口：系统先处理确定性规则，运营只接真正需要人判断的新开发对象，避免把历史邮件和系统排除项转成人工劳动。

## 根因

- 待审重算只处理“自动通过”和“继续人工审”，没有把“已有原线程”和“确定性排除”作为终态移出队列。
- 历史 8 条参与记录已经落入待审，单纯修代码不会自动清理存量。
- 画像和邮箱数据需要限定记录、限定平台、限定写入条件；不能为追求数量降低质量门槛。

## 改动范围

- `app/launch_runtime.py`：新增待审终态分流，未来同类对象自动退出运营队列。
- `scripts/reconcile_launch_review_routes.py`：只处理审计锁定的 8 条存量参与记录，并逐条回读。
- `scripts/refresh_kol_profile_quality.py`：只刷新食人花 YouTube 画像；Instagram 明确排除，并在正式写入后逐字段回读。
- `app/kol_email_repair.py`、`scripts/repair_kol_email_quality.py`：邮箱修复仅接受验证结果为 `valid` 的地址，带重复邮箱、并发变化和写后回读保护。
- `scripts/run_kol_p0_repair_with_production_env.ps1`：只读生产服务配置到当前进程，不修改云端环境变量。

## 上线前验证

- 专项测试：171 项通过。
- 完整回归：885 项通过；1 项既有固定日期 Zeabur 看门狗测试失败，与本次改动无关。
- 存量分流演练：Dave 7 条、食人花 1 条均精确命中。
- 食人花画像演练：37 条 YouTube 中 36 条正常、1 条抓取失败；5 条 Instagram 因云端刷新器不支持而排除。
- 邮箱小样本：先跑 20 条，`valid=0`，因此本次不写任何邮箱。
- 画像回读兼容飞书数字字段以文本返回（例如计划值 `2`、回读值 `"2"`），只比较业务值，不把返回格式差异误报成写入失败。

## 生产执行结果

- 代码通过 fast-forward 合入 `master`，Zeabur 已按目标提交完成部署，`/health` 返回 HTTP 200。
- Dave：7 条错误进入“新开发待审”的历史线程对象已全部退出运营队列，改为沿原邮件线程处理；7/7 回读一致。
- 食人花：`HandheldBOSS` 已系统排除，不再占用运营审核；1/1 回读一致。
- 食人花画像：37 条 YouTube 记录已写入，36 条正常完成、1 条保留明确的抓取失败状态；37/37 逐字段回读一致。
- 食人花 Instagram：5 条明确排除于云端 YouTube 刷新器，未误写。
- 邮箱：20 条小样本没有任何 `valid` 结果，正式写入为 0；没有为了追求数量接受 unknown 或公开但未验证地址。

## 安全边界

- 不发送开发信、回复邮件或飞书运营卡片。
- 不修改 n8n、环境变量或本地部署终端。
- 不降低国家、语言、内容、历史合作或邮箱质量门槛。
- 精确业务 cohort 只保存在本地审计文件，不提交到代码仓库。

## 剩余风险

- 食人花 5 条 Instagram 资料仍需本地爬虫能力，不在本次云端修复范围。
- 邮箱 20 条小样本没有得到任何有效结果，说明当前 Snov/YouTube 公开资料路径不能解决 Dave 的大批邮箱缺口；应先补充数据来源，再做下一批，不应盲写或放宽为 unknown。
