# 集中宣发三项 P0 修复交接（2026-08-21）

## 结论

本轮按顺序完成：食人花补池真实产出修复、两条错误频率定时任务修正、承诺/上稿结果闭环。真实邮件仍只由原发送中心处理，本轮没有改邮件模板或直接发送邮件。

## 1. 食人花补池真实产出

- 问题：只刷新旧资料、没有新增候选/任务/草稿时，任务仍可能报告供给进行中。
- 根因：`profile_refresh_writes` 被错误计入真实供给进展；外部拓词服务402时没有可控降级来源。
- 修复：资料刷新只作为观察数据；食人花活动在动态拓词不可用时使用受控的en/de/es长尾词池，仍经过原国家、语言、内容、重复触达和人审规则。
- 生产验证：`launchruntime-cef5ebdacaa5`真实创建5条待触发发现任务，飞书爬虫任务台回读en2/de2/es1。
- 版本：`6750be9`、`222aada`。

## 2. 两条定时任务

| 工作流 | 修正后时区 | 修正后cron | 当前状态 |
|---|---|---|---|
| `ugM1hX94RrzDWmhj` 草稿归档清理 | `Asia/Shanghai` | `0 4 * * 1` | active，3节点 |
| `0wViUZQ6nyJpNtMJ` 上稿登记卡 | `Asia/Shanghai` | `30 10 * * 1` | active，3节点 |

修改按“GET完整工作流→只改目标字段→停用→PUT完整JSON→重新启用→回读”执行。2026-08-24周一仍需回读首次真实execution；当前只确认配置，不把配置回读当成真实执行验收。

## 3. 承诺/上稿结果闭环

- 新模块：`app/launch_outcomes.py`。
- 新接口：`POST /launch/outcomes/reconcile`，默认`dry_run=true`；正式写入受`LAUNCH_ACTIVITY_QUEUE_ENABLED`保护。
- 承诺事实：先剥离英/德/西语引用历史，只接受本轮回复里“明确发布动作+同句/同分句具体日期”。
- 上稿链接：只用活动关联草稿的`live_link_received`，并验证为视频/帖子/文章等内容级URL；频道主页、媒体官网首页和栏目页不算。
- 实际上稿时间：同一回复还必须明确表达“已经发布+具体日期”；邮件回复时间不再冒充发布时间。
- KOL主表历史上稿不自动归因到活动，因为它是跨品牌全局字段，“当前只有一个活动”也不构成产品/活动证据。
- 活动隔离：无论调用方是否预传名单，写前都精确复核活动ID；产生事实的源草稿必须唯一归属当前活动参与记录，禁止共享草稿跨活动回填。
- 写入验证：每条更新后立即按record_id回读日期和URL；不一致会暂停扩池、标记后台任务`degraded`并告警，失败参与记录ID保留在任务摘要。
- 容量：两活动在60秒内通过并发单飞锁复用同一份草稿快照；同一次反馈的结果回填和指标计算也复用同一批数据，不再双重扫描。
- 控制指标：`ontime_posts`只由`实际上稿时间`计算，不再用承诺日期冒充真实上稿。
- 自动接入：每轮活动反馈或自治补池在做扩池/停止判断前先同步结果。
- 生产回放：Dave扫描182条、食人花扫描43条；当前均0条满足严格事实口径，因此0写入、0错误、0歧义。这是正确的“暂无可核验成果”，不是漏写。
- 版本：首版`1e323d3`，事实边界修正版`aed1da3`；最终审查加固版随本交接提交部署。

## 验证

- 活动相关测试：最终195/195通过。
- 全仓测试：459/460通过；唯一失败是既有`test_zeabur_watchdog`部署日期窗口fixture，与本轮改动无关。
- 生产dry-run后再commit回放，两活动均无错误且无误写。

## 剩余风险与下一步

- 2026-08-24周一回读两条定时任务的首次真实execution和业务产物。
- 现有回复没有明确承诺日期，活动停止线仍为0；系统会继续按原质量标准扩池，这是设计预期。
- 现有人工上稿登记卡只写KOL主表，并以点击时间写日期；下一步需要让运营在卡上确认活动/产品并填写真实发布日期，再明确写到活动参与记录。该项列为11.6A，当前不会用唯一活动规则猜测归属。
- 曝光、订单、GMV、费用、毛利归因仍是P1，未在本轮扩大范围。
