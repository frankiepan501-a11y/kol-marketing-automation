# KOL 数据看板容量 P0 治理交接（2026-08-19）

## 结论

`KOL营销数据看板` 已从 18,959 行降至 8,795 行，释放 10,164 行容量。每日刷新工作流已恢复运行，后续自动执行相同留存规则：

- 近 30 个自然日保留全部维度明细；
- 更早历史只保留每个 ISO 周和每个自然月最后一个有数据日的“总览”行；
- 日期无效的记录不自动删除，只计数并暴露给检查结果；
- 当日刷新先创建 324 条新快照，再删除 324 条旧快照，创建失败时不会清空当天数据。

## 问题与根因

- 治理前行数：18,959 / 20,000，约 95%。
- 原 `app/dashboard.py` 每天追加约 324 行，只覆盖当天，不清理历史；预计约 3 天触顶。
- 接近上限后，单表全量读取实测约 193 秒；完整每日刷新读取约 2.8 万条跨表记录，实测约 11 分钟。同步 HTTP 请求会被外层网关提前断开，因此必须采用“立即确认已接收 + 后台执行 + 状态查询”。

## 安全执行记录

1. 暂停 n8n 工作流 `up7JBPEhvaM3jfJq`。
2. 全量导出 18,959 行和 13 个字段，验证记录 ID 唯一且数量一致。
3. 备份路径：`D:\Documents\独立站\backups\kol-dashboard\2026-08-19-before-retention`。
4. 记录压缩包 SHA256：`3daf6229f0d2cc69da182427fa07ce8f0fc0581c9f901a3935f5cb5fd20c67db`。
5. 本地备份回放与生产 `dry_run` 结果完全一致：计划删除 10,164 行，异常日期 0。
6. 显式 `mode=commit` 后台任务实际删除 10,164 行；飞书回读 8,795 行。
7. 正常每日刷新生产回归成功：生成 324 条、替换今日旧记录 324 条、额外历史删除 0、异常日期 0；回读仍为 8,795 行，表版本由 296 更新至 300。
8. 恢复 n8n 工作流并回读 `active=true`。

## 改动文件与版本

- `app/dashboard.py`：留存计划、批量清理、默认只预览维护逻辑、正常刷新安全替换顺序。
- `app/main.py`：`/dashboard/retention` 和 `/dashboard/refresh` 改为可查询后台任务。
- `scripts/export_bitable_table_backup.py`：通过 lark-cli 全量导出、唯一性/数量/SHA256 校验。
- `tests/test_dashboard_retention.py`：留存边界、默认 dry-run、显式 commit、后台任务状态测试。
- commits：`67c0080`、`840afe3`、`1992350`。
- 生产部署：commit `1992350c8bc606c50731e10525390f5ad21dec2d`，Zeabur deployment `6a8545c67bdd315d2c839c8f`，状态 `RUNNING`。

## 验证

- 留存专项测试：7/7 通过。
- 全仓测试：246 通过；唯一失败是本次未改的既有 `test_zeabur_watchdog`。
- 生产 `dry_run` 与备份回放数字完全一致。
- 生产 commit、正常刷新、行数、版本号、n8n active 状态均已回读。

## 剩余风险与建议

- P1：正常看板刷新实测约 11 分钟，虽然已不受同步网关限制并且可查询状态，但仍应后续做字段裁剪、并行只读或增量聚合，降低 API 调用量。
- P1：n8n 目前只负责启动刷新；后续监控卡应查询 `/dashboard/refresh/jobs/{job_id}` 的最终状态，而不是只看 n8n execution success。
- P2：备份当前在本地工作区，建议后续纳入公司备份目录的统一保留策略；不要把业务数据复制到 memory 或 Obsidian。

