# KOL 活动竞品证据与参与记录交接

状态：P0 三池修复和活动队列上线已完成。Dave 已进入正式自动放量；食人花已完成配置、建池和测试邮箱 Raw 验证，但20名候选仍在人审闸内，因此尚未向真实 KOL 发送。

## 已实现

- 活动创建支持三种竞品证据模式：发起新分析、引用历史证据、不使用竞品证据。
- 新分析支持 `start → submit → confirm`，失败后可 `retry`；每次写操作校验证据配置版本。
- 只读候选预览与单条回放支持 `campaign_id`；证据失效时关闭竞品加分，但保留基础预览。
- 全池预览默认立即返回 `job_id` 并后台计算，状态接口返回最终结果；相同参数重复点击复用运行中的 job，避免重复扫描。
- NYXI 证据只在指定的 Dave 活动、指定产品主记录、对象类型为 KOL 时参与排序。
- NYXI 专用口径：排除已登记官方渠道后，非官方帖子默认作为合作 KOL 证据；保存 `rule_inferred_non_official` 来源，不篡改原表人工复核状态。
- 活动参与记录按 KOL/媒体人分别锁定完整名单；失败时区分可自动重试和需人工修复。
- 已锁定记录保存最多 10 版排序快照，单条回放优先显示当前名单版本对应的历史快照。
- 排序快照保存稳定身份键、长期合作首末帖子、P75 门槛/样本量和基础筛选结论，后续可解释“为什么这个人排在前面”。
- 新建参与记录后立即按 `参与记录ID` 回查唯一性；写入结果不确定或发现重复时进入人工阻塞，禁止自动重试。
- 表结构脚本默认 dry-run；只新增，不删除、不重命名、不覆盖同名异类型字段。

## 安全边界

- `LAUNCH_EVIDENCE_ENABLED` 默认 `0`。
- `LAUNCH_PARTICIPATION_WRITE_ENABLED` 默认 `0`。
- 本期没有创建 KOL/媒体人任务、邮件草稿、卡片、寄样单或真实邮件的代码路径。
- 未修改 `auto_send`、`reply_monitor`、`followup`、`ship_recon` 或 `sla_check`。
- 生产环境长期只开启 `LAUNCH_EVIDENCE_ENABLED=1`；`LAUNCH_PARTICIPATION_WRITE_ENABLED=0`，公开接口不能写活动参与名单。G3 只在受控本地进程中临时覆盖该开关，进程退出即恢复，云端值从未改为 `1`。

## G1 生产表结构只读预检

- 报告：`docs/launch_evidence_schema_dry_run_2026-08-19.md`。
- 活动影子表存在，需新增 19 个字段；节点影子表存在，需新增 9 个字段；参与记录表尚不存在，需新建 26 个字段。
- 全部为新增项：同名同类型复用 0、同名异类型冲突 0、删除 0、改名 0。
- 发现节点迁移定义漏了 `节点状态 / 节点阻塞说明 / 竞品品牌 / 目标证据配置版本`，已先修正本地脚本并补测试。
- 按生产实际字段补齐稳定身份匹配：KOL 使用 `YouTube频道ID / 主链接`，竞品帖子使用 `KOL平台ID / KOL主页URL / KOL账号Handle`。
- G1 全程只读；活动表仍为 2 行/rev 12，节点表仍为 16 行/rev 1，全库仍为 27 张表。

## G2 表结构兼容说明

- Base v3 服务端不接受关联字段的旧 `multiple` 创建参数；第一次提交在第 5 个字段被拒绝并立即停止，前 4 个普通字段已保留，未写业务记录。
- 修正后所有关联字段按飞书当前默认关系类型创建。`关联活动 / 关联KOL / 关联媒体人` 的业务单值与唯一性继续由 `参与记录ID`、活动级进程锁、创建后唯一键回查和人工阻塞规则强制保证，不依赖 UI 字段基数。

## G2 生产状态

- 活动表已新增 19 个字段，节点表已新增 9 个字段。
- 活动参与记录表已创建：`tblt0zD4hDb7sFqn`，26 个字段；G3 后 Dave 当前名单为 20 行。
- 生产环境已设置 `T_LAUNCH_PARTICIPANT=tblt0zD4hDb7sFqn`。
- NYXI 全部 3,423 条帖子中，已识别官方渠道 435 条、非官方 2,988 条；首轮只读测试从非官方记录中选择 60 条高曝光帖子，覆盖 40 位创作者，同一创作者最多 3 条。

## 本地验证

- 活动专项：`test_launch_*.py` 72/72 全部通过。
- 全仓：286 项中 285 项通过；唯一失败为本次未改动的 `test_zeabur_watchdog.ZeaburWatchdogTests.test_run_once_alerts_any_project_service_failed_deployment`。该失败与 G1 前一致。
- `py_compile` 通过。
- `git diff --check` 通过（仅有仓库现有 LF/CRLF 提示）。
- P0/P1 人审增量最终专项 51/51 通过；当前全仓 304 项中 303 项通过，唯一失败仍为上述既有 Zeabur watchdog 用例。

## 代码审查收口

- “不使用竞品证据”若仍带竞品品牌、帖子或事件，现直接拒绝，不再静默清空旧值。
- 调查启动先更新研究节点、最后提交活动版本；启动失败写入失败/阻塞状态但不错误推进成功版本，`retry` 会重新打开节点。
- 参与记录新建后做唯一键回查；无法确认是否成功的请求不会进入自动重试。
- 当前 Zeabur 服务按既有单实例前提使用进程内活动锁。若以后扩为多实例，G2 前必须增加跨实例互斥或数据库级条件提交，不能直接水平扩容写名单。

## 生产执行闸门

### P0/P1 人审可执行化（2026-08-19）

- 活动表新增「活动目标国家」和「活动目标语言」；Dave 试点锁定为美国＋欧洲国家，语言仅 `en/de/es`，活动范围优先于产品库更宽的销售国家。
- 参与记录表新增达人主页、国家/语言/平台/粉丝快照、近期内容、历史关系、竞品证据、审核分流、审核结论/理由/人/时间。
- 分流规则：确定性检查全部通过的普通英语达人由系统建议通过；德/西语、资料缺失/过期、历史复联交 KOL 运营；头部 KOL 或已有付费报价交 Frankie 做例外取舍。
- 每条待审记录必须直接给出主页/主证据帖子链接、系统已检查项和唯一待判断问题；不再使用“请审核20条是否合适”这类笼统交接。
- 这条规则已提升到全局 `AGENTS.md`，后续采购、广告、客服、KOL 等所有人审 gate 共用。
- G4 继续关闭；本次只改审核数据和影子名单，不建任务/草稿，不发邮件，不寄样，不产生付费承诺。
- Dave 当前名单使用批次 `p0-review-v2-20260819g`：20 人已入围、3 人因不在活动国家/语言范围而取消；国家分布为 US 14、UK 5、DE 1，语言为 en 19、de 1。
- 审核分流为：系统建议通过 1 人、KOL 运营审核 17 人、Frankie 例外审核 2 人。达人主页 20/20 可直接打开，2 位 NYXI 证据命中者另有主证据帖子链接。
- 飞书专用视图：`戴夫活动｜人工审核`（`vewH5ud840`），固定筛选当前活动、KOL、已入围，按审核分流分组、最终优先级降序，只显示 22 个判断/回填字段。
- 当前活动字段若存在但国家或语言为空，系统会停止入围；字段完全不存在的旧活动仍按原产品范围运行，避免历史任务被新字段升级意外打断。

1. G1：已完成。只读确认活动表新增 19 字段、节点表新增 9 字段、参与记录表新建 26 字段；无冲突、无生产写入。
2. G2：已完成。Dave 活动为 `引用历史证据 / 已就绪 / evidence-v1 / 配置版本1`，三组只读对照均为 `writes=0`。
3. G3：已完成。提交 Dave 影子 KOL 名单 20 人，名单版本 `evidence-v1`、批次 `g3-dave-evidence-v1-20260819b`；19 条新建、1 条失败保护后原位恢复。写后立即关闭名单授权。
4. P0/P1：已完成。活动级市场硬筛、人审字段/分流/视图、三池隔离和全局人审交接规则已落地；当前名单版本为 `evidence-v3`。
5. G4：真实开发信、付费、寄样和运营群卡片仍需新的明确授权。

## 回滚

- 代码层：两个功能开关设为 `0`；旧产品级只读预览继续可用。
- 表结构：本期只新增。停止使用新增表和字段即可，不做破坏性删除。
- 参与名单：KOL 与媒体人分别按失败批次恢复；人工阻塞状态禁止自动重试。

## 待填写的生产事实

- G2 部署 commit：`73f38db36866161e2f166a383145a996310ae599`，deployment `6a8571b5764188cac9d773f7`。
- 参与表 ID：`tblt0zD4hDb7sFqn`。
- 新增字段：活动 19、节点 9、参与记录 26，均已回读验证。
- Dave 证据配置版本：`1`；排序版本：`evidence-v1`；关联帖子 60、营销事件 7。
- Dave 首个名单版本：`evidence-v1`，20 人，16 人走“同线程激活”、4 人走“新开发”；竞品证据等级 A=2、B=1、无加分=17。
- G3 首次批次 `g3-dave-evidence-v1-20260819a` 在第 1 条回读时因飞书数字字段返回字符串而被保护中止；该条被标记“锁定失败”，其余 19 条未写。修复 `754aeb38227c8688b1bf3e97b6ea9de45c6de9fe` 统一数字回读比较后，用新批次完成，失败记录保留审计轨迹后原位恢复。
- G3 回读：20 个参与唯一键、20 个 KOL 关联，批次和版本均一致；关联 KOL 任务、媒体人任务、邮件草稿全部为 0。
- G3 收口：`名单锁定授权=false`，KOL 阻塞/失败批次/待处理字段为空；发送邮件、样品寄送、付费承诺、储备金释放授权均为 `false`；云端 `LAUNCH_PARTICIPATION_WRITE_ENABLED=0`。
- G3 回读修复已部署：commit `754aeb38227c8688b1bf3e97b6ea9de45c6de9fe`，Zeabur deployment `6a857bcd764188cac9d7771c` 为 `RUNNING`，`/health=ok`；生产名单写接口返回 HTTP 403“活动参与记录写入开关未开启”。
- P0 受控重锁：批次 `p0-review-v2-20260819g`，20 条复用升级、3 条取消；活动 `KOL已锁定名单版本=evidence-v2`，锁定授权/阻塞/失败批次/待处理均已清空。
- P0 外部动作核对：20 条当前参与记录的 KOL 任务、媒体人任务、邮件草稿关联全部为 0；活动的发送邮件、样品寄送、付费承诺、储备金释放授权全部为 `false`。
- P0/P1 人审实现已部署：commit `3a7b156925689df541aab3fcaeb991a7e7b53a96`，Zeabur deployment `6a858f11764188cac9d77c15` 为 `RUNNING`，`/health=ok`；云端 `LAUNCH_PARTICIPATION_WRITE_ENABLED=0`，生产名单写接口仍返回 HTTP 403。

## 2026-08-19 P0 三池修复与生产回填

- 根因修复：`reactivation_same_thread` 不再与新开发候选共用入围资格。候选被明确分为 `新开发池 / 现有流程贡献池 / 二次发布池 / 已排除`；活动新开发目标只允许 `eligible_new_cold` 补足。
- 人工审核闭环：人工选择“排除”后，记录会退出当前可执行名单；系统只从新的冷开发候选中补位，不再让已洽谈、已寄样或已上稿对象占用 20 人的新开发目标。
- 二次发布口径：历史已上稿对象默认进入二次发布池，只有取得“本次窗口再次发布”的明确承诺后，才可计入本次活动的发布目标；不会自动重发 cold 开发信。
- 地区一致性：除结构化国家/语言外，增加近期内容地区冲突检查；同一非目标国家反复出现时冻结候选。单条旅行或偶发提及不会直接误杀。
- 表结构：参与记录表新增单选字段 `活动分池`，仅新增字段，无删除、重命名或覆盖。
- 生产批次：活动 `launch-20260915-funlab-dave-ys11-5` 使用名单版本 `evidence-v3`、批次 `p0-three-pool-v3-20260819a`。回填新建 16、复用 4、取消旧入围 16、标注历史分池 19。
- 最终回读：当前有效名单 20 人、唯一 KOL 20 人；20/20 均为 `活动分池=新开发池` 且 `进入方式=新开发`。其中人工通过 8 人、待 KOL 运营审核 12 人。
- 历史分池回读：二次发布池 4 人、现有流程贡献池 8 人、已排除 7 人。Amrie47 已为 `已取消 / 已排除 / 运营取消`，不在当前 20 人中。
- 外部动作核对：当前名单关联 KOL 任务、媒体人任务、邮件草稿均为 0；活动的名单、邮件、寄样、付费、储备金授权均为 `false`。云端写开关为 `0`，生产名单写接口以 HTTP 403 拒绝写入。
- 验证：活动专项测试 96/96；全仓 309 项中 308 项通过，唯一失败为本期未修改的既有 Zeabur watchdog 用例；`py_compile`、`git diff --check` 通过。
- 上线证据：代码 commit `bd7af3fff38cd92e333e70572398bb9e9eed6406`；Zeabur deployment `6a85bbbf2a82f89733776800` 为 `RUNNING`，`/health=ok`。

## 2026-08-19 NYXI 全证据、3 人补位与 G4 验证收口

### NYXI 全证据接入

- 生产硬口径：帖子 3,423 条；官方渠道 435 条；非官方合作证据 2,988 条；非官方作者去重后 1,457 位。
- 飞书单个关联单元格写入 2,988 条关系会返回 `1254001 WrongRequestBody`，且本次失败未产生业务变更。因此改为 50 个有序证据快照节点：前 49 个各 60 条，末块 48 条。
- 每个节点均为只读证据，`允许外部动作=false`；系统校验分片编号连续、总数准确、无重复和有序帖子 ID 的 SHA-256。只有 50 个节点全部回读准确后，才把活动原子升级到 `evidence-v4 / 配置版本2`。
- 活动主表继续保留 60 条代表帖子和 7 个营销事件，避免高基数关系字段再次失效；完整排序从 `activity_node_snapshot` 读取。
- 后台预览 job `launchpreview-5e7044f8f01d` 在 14分37秒内完成，确认使用 2,988 条有效证据、1,457 位作者；KOL 池 7,720，实际评估 1,232，得到 495 名可用新开发对象、10 名现有流程对象和 4 名二次发布对象。

### 3 人自动补位

- 修复了历史取消记录被误算为“本轮流出”的问题：历史取消对象仍参与防重复，但不计入当前流出；只有当前 `已入围/锁定准备中` 才参与本轮名单差额计算。
- 生产补位结果：保留人工通过 17 人，取消本轮人工排除 3 人，补入 3 名新开发对象，当前有效名单恢复为 20 人。
- 新补对象：`IndieAlpaca`（美国/英语）、`La Poción Roja`（西班牙/西语）、`Mundatos`（西班牙/西语）。三人均为 `已入围 / 待审核 / 新开发 / 新开发池 / evidence-v4`，没有自动越过人工审核。
- 回读核对：有效名单 20、异常记录 0、关联 KOL/媒体人任务 0、邮件草稿 0；名单、真实邮件、寄样、付费和储备金授权均为 `false`。

### G4 测试邮箱 raw 验证

- 使用 Dave 产品 `recvkJOoCsNb1s` 和现有 cold 草稿样本 `recvkNpBRMAyNV`，临时用单变量方式设置 `EMAIL_DRY_RUN_TO=frankiepan501@gmail.com`；设置前后环境变量总数均为 153，未全量覆盖环境。
- 只发送 1 封测试邮件到测试邮箱，run key 为 `dave-nyxi-v4-20260819a`，Zoho message ID 为 `1787157814222138300`；未写生产草稿记录。
- raw 检查 9/9 通过：测试收件人、FUNLAB 发件身份、原主题、dry-run 前缀、正文未截断、HTML 渲染、Dave 产品身份、全部链接、无未替换占位符。
- 验证后已清空 `EMAIL_DRY_RUN_TO` 并重新部署；再次调用测试端点返回 HTTP 400 `EMAIL_DRY_RUN_TO 未设置；禁止执行邮件测试`，证明测试通道已关闭。

### 版本与验证

- 最终代码 commit：`73826ec30d357d7cdeab964ca63b3ceb86f4ae8f`。
- 最终运行部署（清空测试邮箱后）：`6a85dd4bf1ea67ebf4ea66a9`，状态 `RUNNING`，`/health=ok`。
- 活动专项 113 项和 3 个子测试通过；全仓 333 项中仅既有 `test_zeabur_watchdog.ZeaburWatchdogTests.test_run_once_alerts_any_project_service_failed_deployment` 失败，与本次改动无关。
- 独立审查已确认：分片续跑/回滚、连续性/数量/哈希校验、Dave 硬口径、生产版本复核及名单闸门不覆盖证据版本均已收口。

### 当前下一步

- 先由运营审核新补的 3 名达人；在审核通过并取得 Frankie 对“首封真实邮件”的单独授权前，不允许创建真实发送批次。
- 真实上线仍按固定顺序：测试邮箱 raw 通过（已完成）→ 1 名真实 KOL 灰度 → 回查发件箱 raw → 再决定是否批量。当前停在第一步完成、等待真实灰度授权。

## 2026-08-20 双品牌活动队列与正式放量

### 已上线

- 现有 `auto_send` 继续作为唯一真实发件出口；活动草稿必须同时通过正式模式、邮件授权、名单版本、活动参与记录、产品活动锁、全局重复触达、Raw证书和Zoho滚动24小时额度检查。

## 2026-08-20 P0：活动无人值守自治补池

- 新入口：`POST /launch/runtime/autonomous-refill`。它只负责检查活动承诺缺口、Zoho 滚动24小时余量、活动可发送草稿库存，以及补候选/刷新资料/建草稿；不直接调用 Zoho 发送。
- 安全边界：停止和保持仍由活动自己的“明确承诺数 / 预计按时上稿数”决定；所有国家、语言、平台、粉丝范围、内容画像、邮箱和全局重复触达规则保持原阈值，返回值固定记录 `quality_filters_lowered=false`。
- 双路并行：已有“待审核/待补资料”记录不再阻塞系统明确合格者自动进入活动队列。机器明确通过者不重复交人工；资料缺失或语义边界项只进入 KOL 运营审核视图，不生成草稿。
- 自动拓新：主表可用候选不足时，先刷新最多30名现有 KOL 的近期画像；仍不足则在爬虫任务台建立带 `[活动补池:<campaign_id>]` 前缀的 YouTube 活动专属任务，由现有本地 daemon 入库，下一小时继续使用。
- 可观测与可恢复：后台任务状态以 `[AUTONOMY_JOB]` 单行 JSON 持久写入活动表 `数据口径备注`。服务内存重启后，`GET /launch/runtime/jobs/{job_id}?campaign_id=...` 仍能回读 `running/success/error`；45分钟无更新视为失联，下一轮允许接管重跑。
- 运营通知：只有新增边界项时，按飞书人事“独立站运营专员”实时查在职审核人，发 KOL·P2 卡并直达对应产品审核视图；卡内明确系统已检查项、人工只需判断的维度和回填位置，Frankie 无需逐条审核。
- `POST /auto-send/run?async_mode=true` 改为立即返回后台任务ID，`GET /auto-send/jobs/{job_id}` 查询结果；运行中相同批次会复用任务，不重复发送。
- n8n工作流 `UIShaANEi8M0rx1v` 保持每10分钟运行并已切换后台模式；每日反馈工作流 `VWTMNkXf0zcs4Kvz` 每天北京时间17:00依次评估Dave与食人花。
- 线上代码版本为 `c6dc0ec`。活动专项测试：`test_launch_routes` 10项、`test_launch_runtime` 7项、邮箱清洗3项全部通过。

### Dave生产结果

- 活动：`launch-20260915-funlab-dave-ys11-5`；参与记录142条，已关联活动草稿120条。
- 四个真实批次各发送6封，全部 `sent=6 / fail=0 / lock_validation_failures=0 / zoho_count_errors={}`；加上IndieAlpaca灰度，活动累计已发25封。
- 第四批任务 `autosend-45da78e9670c` 于 `2026-08-20 07:23:04 UTC` 完成；当时FUNLAB滚动24小时总外发39封，系统按总上限120计算剩余81封，活动待发96封。
- 邮件仍不包含未确认价格、KOL Deals、佣金或付费承诺；样品、付费和储备金授权全部关闭。

### 数据事故与修复

- 首批发现一条爬虫邮箱被网页问候语粘连为 `.comHowdy`。Zoho已接受该地址，无法撤回，预计会退信。
- 系统已立即暂停Dave、修复 `app/feishu.py::clean_email` 并纠正KOL主表邮箱；同一邮箱存在多个抓取版本时只取干净短版本，只有粘连版本时直接拒绝。
- 修复测试和线上部署确认后才重新开启Dave。后续批次收件地址均通过新规则。

### 食人花当前闸门

- 活动：`launch-20260915-powkong-piranha-v2`；产品、英语主区及德/西语辅助市场、无竞品证据模式、名单版本和Raw证书均已配置。
- 20名候选已进入活动参与记录：18名为百万级以上头部，需要Frankie例外审核；2名近期内容资料超过180天，需要KOL运营审核。
- 这20名尚未通过“对象是否适合本活动”的人审，因此系统保持 `发送邮件授权=false`。正式放量授权不会覆盖候选适配审核；审核通过后无需改代码，队列即可进入同一发送中心。

### 当前授权边界

- 已授权并执行：活动队列、Dave扩池、食人花配置与建池、测试邮箱回放、Dave正式放量、每日反馈控制。
- 未执行：样品寄送、付费合作、储备金释放、公开活动价/KOL Deals承诺。
