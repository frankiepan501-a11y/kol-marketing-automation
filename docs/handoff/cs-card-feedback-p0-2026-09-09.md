# 客服卡片假闭环与资源误匹配修复（2026-09-09）

## 结论

张佳烨反馈的三个现象均已在生产证据中复现：

1. 物流邮件中的普通英文 `update` 被当成“固件更新”，导致物流卡被整套 FUNLAB 固件资料和固件回复覆盖。
2. 同一次飞书点击会同时送达新、旧两种回调载荷；原回调在网络读写完成后才返回，生产样本耗时 2.3～6.1 秒，前端因此报 `200341` 超时。
3. `CS_REPLY_LIVE=0` 时卡片仍显示“发送回复给客户”。点击后代码不发邮件，却把工单状态写成“已回复”并把卡片改成绿色结果态，形成假闭环。

## 影响

- 两条 2026-09-09 点击过的邮箱工单被错误标成“已回复”，但工单表没有回复时间/出站 Message-ID，网易已发送也没有给这两位客户的邮件。
- 全表发现 11 条物流工单的 AI 草稿被固件资源回复污染，其中 10 条仍在待回，1 条已处理。

## 代码修复

- `app/cs_resources.py`：只在出现 `firmware`/固件/升级工具等明确证据时才匹配固件资源；普通 `update` 不再触发。
- `app/cs_resources.py`：FF05 的“振动/重置”症状本身也不再自动解锁固件；物流工单是固件资料的硬排除项。
- `app/cs_dispatch.py`：
  - 真发送关闭时不渲染发送表单/按钮；
  - 旧卡的发送按钮即使被点击也会失败闭锁，不写“已回复”；
  - 真发送/DRY-RUN 按钮回调先即时确认，后台再读表、校验和发送；
  - 在任何网络读取前建立同工单发送锁，同一点击的双回调只处理一次；
  - 后台校验失败时 PATCH 原卡为红色“未发送”，保留修正和重试入口。
  - 邮件发送后回查对应已发送箱；只有 Message-ID、收件人和正文完整性都匹配才写“已回复”与“最近出站Message-ID”。若通道已受理但凭证暂未出现，则保持待回、显示“待核实”并阻止立即重发。
  - “待核实”会把 provider Message-ID 作为 `CS_OUTBOUND_PENDING:` 标记写在沟通历史最前面；即使服务重启或历史已满 5000 字，也会继续阻止重复发送。
  - 已拿到真实出站凭证但工单回写失败时，卡片明确显示“客户已收到·勿重复发送”，并尝试第二次最小化写入持久防重标记。
- `app/main.py`：`/cs/callback` 改用快速确认入口。

## 验证

- 先新增回归用例并确认修复前失败：物流 `update` 误匹配、手动模式仍显示发送按钮、旧卡点击会写“已回复”。
- 修复后客服相关 53 项测试通过，并用生产抓取的 legacy / schema 2 两种载荷结构覆盖同一次点击去重；同时覆盖已发送箱正文/链接校验、工单回写失败和 5000 字历史的持久防重。
- 最终全仓回归 1119 项中 1118 项通过，另有 40 组子测试通过；唯一失败是仓库长期已知的 Zeabur 看门狗固定历史日期用例，与客服改动无关。

## 生产处理结果

- 修复提交 `8c55f3e`、`2f9ce5e` 已 fast-forward 推送到 `origin/master`；线上 `/health` 已回读 `cs_card_fix_version=2026-09-09-p0-v2`。
- 两条假“已回复”工单已恢复为“待回”，保留 AI 草稿、清空“最终回复”，且再次核对已发送箱为 0 封。
- 10 条仍待回复的物流工单已清除固件资源与固件回复污染，改为保守的物流答复草稿；已处理的 1 条历史记录未改。
- 原卡均已超过飞书 14 天更新期限，PATCH 返回 `230031`。因此用同一个客服助手 App、同一个原私聊重新发送 12 张替代卡，并把新 `message_id` 回写工单；没有跨 App 复用 `open_id`。
- 最终只读核验 12/12 通过：状态、草稿、资源状态、新卡存在性均正确；所有新卡均为手动回复模式，不含“发送给客户”按钮，也没有向客户发送邮件或 Discord 消息。

## 剩余上线闸

一键真实发送仍保持关闭。Frankie-only 首次测试已完成：测试邮件于 2026-09-09 18:10 投递到 `frankiepan501@gmail.com`，DRY-RUN 提示、订单号 `FL1044`、物流号 `4PX3003078050691CN`、4PX 链接和英文签名均完整；真客户未收到，工单仍为待回。

首次测试同时发现：当前网易账号的 SMTP 发信不会自动出现在 IMAP“已发送”文件夹，导致系统虽然已投递 Gmail，仍因缺少已发送凭证而保持“待核实”。提交 `c115386` 已修复：SMTP 必须明确接受收件人；接受后将同一份原始 MIME 邮件保存到网易“已发送”，再按 Message-ID、收件人、正文和链接回读验证；保存或回读失败时继续保持待回并防重复发送。DRY-RUN 收件人也已限制为 `frankiepan501@gmail.com`，非白名单会在调用邮件服务前失败闭锁。

提交 `c115386` 已推送 `origin/master` 并由 Zeabur 部署为运行中；`/health` 返回 `status=ok`，客服回调进程 `started_at=1788950106`。客服相关 65 项测试通过；全仓 1124 项通过、42 组子测试通过，唯一失败仍为既有 Zeabur 看门狗固定历史日期用例，与客服改动无关。

Frankie 已授权最后 1 封终测。19:14 新测试卡单次点击后，原卡更新为“DRY-RUN已提交”；Gmail 收到新邮件；网易“已发送”按 Message-ID 回读成功。自动检查确认 DRY-RUN 提示、订单号 `FL1044`、物流号 `4PX3003078050691CN`、4PX 链接、英文签名完整且无未替换占位符；相关退信 0。工单仍为“待回”，回复时间、出站 Message-ID、最终回复均为空，真客户/频道发送为 0。

终测前，旧 `CS_OUTBOUND_PENDING:` 锁先成功阻止了一次重复尝试，没有发信。为完成已授权测试，仅删除该测试工单的一条过期防重锁，并在写后确认其他工单字段未变。测试脚本曾因错误要求 `FL1044` 出现在主题而假报 0 条，已改为在正文核对；这不影响生产发送或凭证结果。

至此，一键回复的技术上线闸已通过。生产仍保持 `CS_REPLY_DRY_RUN_TO=frankiepan501@gmail.com`，且 `CS_REPLY_LIVE` 未开启。只有 Frankie 另行确认后，才能移除 dry-run、设置 `CS_REPLY_LIVE=1` 并重新部署；Amazon 缺订单号/站点客户在正式发送前仍未收到补询邮件。

## 正式上线（2026-09-09）

Frankie 已明确授权开启正式发送。Zeabur 使用单变量接口新增 `CS_REPLY_LIVE=1`、删除 `CS_REPLY_DRY_RUN_TO`；变量数 195→195，其他变量键未变，未使用全量覆盖。deployment `6aa1435781898898b92bf563` 在 commit `d3a29e1` 达到 RUNNING；`/health status=ok`，客服回调 ready，`CS_DISPATCH_OBSERVE=0`，后续新工单按「分配运营」路由。

今天恢复的手动卡属于静态消息，不会随环境变量自动增加发送按钮。为完成交接，系统按原负责人张佳烨补发 13 张新正式卡（12 张已修复卡 + Amazon 缺订单号/站点补询卡），使用稳定幂等 key，并逐条回写新 message_id。只读复核 13/13 通过：卡片为正式发送说明、工单仍为待回、回复时间和出站 Message-ID为空；本次客户邮件/Discord 发送 0。真实客户只有在负责人逐卡确认并点击后才会收到，且取得已发送箱凭证后工单才会转为已回复。

飞书消息读取接口会省略交互卡的输入框/按钮动作值，所以不能用 GET 返回里是否有 `cs_send_reply` 验收按钮。当前采用“发送前完整卡结构校验 + 发送后新 message_id/正式模式说明/无 DRY-RUN 提示/工单状态回读”的组合验收。

## P2 外部巡检（2026-09-09）

- GitHub Actions `Zeabur Tokyo Watchdog` 已扩展为客服服务外巡检，定时从飞书客服工单 Base 直读最小字段；不调用 `kol-auto` 客服服务，因此客服服务本身宕机时仍能发现断流和凭证异常。
- 审计对象为 `已回复 / 已解决 / 待客户补充`。正式发送切换后的记录按 `回复时间 / 补充信息请求时间` 判断；历史旧记录只因无关字段被修改不会误告警。
- 状态缓存只保存 `record_id + 状态`。旧邮件线程若从“待回”等状态重新进入需凭证状态而没有新凭证，下一轮会抓到；首次启动或缓存丢失会明确告警“本轮不能判健康”，不会静默重建基线。
- GitHub 只读取状态、业务时间和 `最近出站Message-ID`，不读取客户邮箱、邮件正文或附件；异常卡按每卡最多 6 条分片，每条都有工单链接，并按工单分别冷却，新增异常不会导致旧异常反复刷屏。
- 上线时发现 GitHub 的旧 `ZEABUR_API_KEY` 已失效，且缓存中的旧 summary 会让失败运行误显 success。已更新现有 Secret，并在每次运行前删除旧 summary；脚本致命失败也会强制写入新的失败 summary。Zeabur 查询失败不再阻断客服 Base 巡检。
- 后续终审又发现“Secret 被删除时整项任务会跳过”的缺口，已在 `77f3d99` 移除该跳过条件；即使没有 Zeabur 密钥，客服 Base 巡检仍会运行，Zeabur 缺密钥本身会被记为异常。
- 已在另一套 n8n/Zeabur 编排上部署低频反向监测 `YLVywhEu3lLwXmKo`（`GitHub客服外巡检-Deadman`）：每 30 分钟检查 GitHub 定时巡检是否超过 35 分钟未运行或持续失败，异常每 6 小时最多提醒一次，恢复时通知一次。由 GitHub 巡检 Zeabur、Zeabur 反查 GitHub，避免单边静默。
- 该反向监测已在 13:23 UTC 首次自然触发，execution `1151828` 为 success，并因 GitHub 最近一次 schedule 超时正确发出 1 条内部 P1 告警；没有读取客服数据、写工单或发客户消息。
- 首轮真实运行 `34353965061` 正确告警建立基线并判定 failure；复检 `34354106429` 与最新推送运行 `34354413172` 均扫描 1223 条，`issue_count=0`、`current_anomaly_count=0`、`baseline_was_missing=false`、`alert_sent=false`。
- 定时表达式调整为每小时 `07/17/27/37/47/57` 分执行，避开 GitHub Actions 整点高峰；最终代码提交 `77f3d99`。P2 专项 26 项、全仓 1139 项及 42 组子测试全部通过；最终 GitHub run `34356025686` 扫描 1223 条、异常 0、未发告警。
